# 妙答 Agent 全栈架构演进 · 四方案对比评估

> 分析对象：`miaoda-code-completion-assistant`（v1.1.1，commit 状态见 `agent/package.json:3`）
> 分析范围：模型调用 / Prompt 配置 / 浏览器控制 / 编排循环 / 对外出口，共五层
> 分析日期：2026-09-11
> 方法：先读代码做事实核查，再按四维度量化评分
> **落地状态（2026-09-11）**：方案 C 已在本分支 `feat/2-c-web-service` 以**最小形态**实现（`web-server.mjs` + 单文件原生前端 + `start-web.bat`）。决策修正说明：本文第五节"只做 API、不做 UI"的否决建议被用户明确决策推翻——用户需要的正是网页访问形态；实现上以单文件原生前端控制成本（无 React、无构建链），仅绑 127.0.0.1，不引入"远程/多人"所需的浏览器池与账号隔离，故原分析中"双栈维护成本高"的顾虑已被该形态规避。文档其余部分保持为分析快照。

---

## 一、结论先行

**统一推荐：以方案 D（CLI 分层加固）为底座，叠加方案 A（MCP Server 化）作为增量出口层。二者不冲突，可并行推进。**

| 用途 | 主推 | 理由 |
|---|---|---|
| 自用稳定优先 | **D** | 不触碰已端到端验证的链路，修复成本最低 |
| 求职作品集加分 | **A**（增量） + **D** | 拿到 MCP 技术标签，且改造面最小、可演示、可量化 |
| 纯技术可行性探讨 | **A vs C 的取舍** | 二者是仅有的两条"服务化"路线，分野在于是否重建 UI |

**明确不推荐：方案 B（MCP Client 化）与方案 E（浏览器扩展）。** 前者会丢弃 `act.mjs`（约 46.5 KB）中不可替代的实战资产；后者在 MV3 下无法实现 xterm 逐字符键入，直接砍掉命令行题能力。

**不推荐 C 的 UI 部分**：项目已于 2026-09-09 主动移除 React 工作台（见 `README.md:283`，git 提交 `3cc19d3`）。在没有新需求驱动前重建用户界面，属于重复支付已决策过的成本。

---

## 二、当前已实现的架构（代码事实）

先固化基线。以下全部经代码核查，非推测。

### L1 · 模型调用层 —— 自研 fetch 直连

| 事实 | 证据 |
|---|---|
| 手写 `fetch` 调 OpenAI 兼容 `/chat/completions`，**无任何 SDK** | `ai.mjs:37-198`；`package.json:33-35` 唯一运行时依赖是 `playwright-core` |
| 自研 SSE 流式解析（逐行取 `data:` 载荷） | `ai.mjs:126-159` |
| 自研指数退避重试（`800 × 2^i`，默认 2 轮） | `ai.mjs:59-61, 186-191` |
| 空闲超时（非绝对超时）+ 30s 心跳日志 | `ai.mjs:82-92` |
| **思考硬闸**：思考 >20s 且正文 0 字则主动断流 | `ai.mjs:154-157`；配置 `config.mjs:84` |
| **双通道关思考**：`chat_template_kwargs.enable_thinking` + 顶层 `enable_thinking` | `ai.mjs:66-73` |
| 400 错误时自动降级 `max_tokens` 至 8192 重试 | `ai.mjs:108-114` |

**评价**：这是全项目工程量最重、也最有价值的一层。它把推理模型接入中几乎所有的坑都趟过了——"思考马拉松击穿预算"、"端点挂起无日志"、"忽略思考开关"，每一处都对应真实事故（`ai.mjs:51-53` 记录了 2026-09-10 反思思考 2.5 万字的事故）。

### L2 · Prompt 配置层 —— 单一数据源

| 事实 | 证据 |
|---|---|
| 7 个能力配置存于仓库根 `shared/capabilities/*.json` | 目录实测 7 个能力文件 |
| `renderTemplate` 渲染 `{{input.xxx}}`，缺失渲染为空串 | `ai.mjs:28-32` |
| `readCapability` 按 id 直接读 JSON | `ai.mjs:19-25` |
| **硬约束**：代码内不得复制 prompt | `AGENTS.md`「prompt 单一数据源」 |

### L3 · 浏览器控制层 —— playwright-core over CDP

| 事实 | 证据 |
|---|---|
| `connectOverCDP` 接管用户已登录浏览器 | `browser.mjs:44-83` |
| 连接前**临时摘除 6 个代理环境变量**（本机 http_proxy 导致 502） | `browser.mjs:21-36` |
| 连不上时自动拉起独立 profile | `browser.mjs:57-78` |
| `my-edge` 用目录联接（junction）绕过 Edge 136+ 默认目录禁令 | `launch-browser.mjs`（11 KB） |
| 保留端口区间（9137–9236）自动顺延 | `config.mjs:62-65` |
| 感知 40 KB / 执行 46.5 KB，全部直接调 Playwright page API | `perceive.mjs` / `act.mjs` |

### L4 · 编排层 —— 手写循环

| 事实 | 证据 |
|---|---|
| 单题核心函数 `solveOnce(page, probe)` | `loop.mjs:184` |
| 四个模式各自独立管理浏览器生命周期 | `runLoop:600` / `watchLoop:709` / `liteLoop:777` / `courseLoop:917` |
| **每个 loop 在 finally 里 `browser.close()`** | `loop.mjs:630` |

### L5 · 对外出口 —— 单机 CLI

| 事实 | 证据 |
|---|---|
| 11 个 CLI 子命令 | `cli.mjs:42-178` |
| 5 个 Windows 双击入口（`.bat`） | `agent/start-*.bat` |
| `withPage` 在 finally 里 close 浏览器 | `cli.mjs:31-39` |
| **无 HTTP 服务、无协议出口、无 UI** | 全仓无 server 相关依赖 |

---

## 三、四个候选方案

### 方案 A · MCP Server 化（把本 Agent 变成可被宿主驱动的工具集）

**思路**：新增 `agent/src/mcp-server.mjs`，用官方 `@modelcontextprotocol/sdk` 以 stdio transport 启动，把 `perceive` / `act` / `solveOnce` 包装成 MCP tools。宿主（Claude Desktop / Cursor / Cline / 自研 Agent）即可直接调用"读题—作答—评测"能力。

**作用面**：L5 新增出口、L4 全量暴露、L2 上抬为 MCP Prompts 原语、L1 部分（模型调用可交给宿主，也可保留自研）、L3 部分（需重构生命周期）。

**关键优势**：纯增量。新增文件不动现有 CLI，`start-watch.bat` 等 5 个入口全部照常可用。

### 方案 B · MCP Client 化（本 Agent 反向消费 MCP 工具）

**思路**：本 Agent 作为 MCP 客户端，接入 `playwright-mcp` / `chrome-devtools-mcp`，把 `browser.mjs` + `act.mjs` 替换为 MCP 工具调用。

**作用面**：L3 全量替换、L4 部分改造。

**致命问题**：`playwright-mcp` 走的是「无障碍树 snapshot + ref 定位点击」范式，与本题场景需要的三样能力完全不兼容：
1. Monaco `model.setValue` 字节级写入（`AGENTS.md` 记载 `insertText` 会被 `formatOnPaste/autoIndent` 逐行重排导致评测不匹配）；
2. xterm 终端逐字符键入 + 自适应提示符等待（`config.mjs:122-130`）；
3. 平台"代码栏双重执行 + echo 双引号提取"机制的确定性适配（`ai.mjs:621-653`）。

这三项是 `act.mjs` 累积的真金，换成通用 MCP 工具等于放弃。

### 方案 C · HTTP API 服务化 + 前端工作台

**思路**：`agent/` 起 Fastify/Express 服务，暴露 REST + SSE 端点；前端做可视化工作台（日志流、题目预览、人工干预）。本质是恢复 `3cc19d3` 的路线并服务化。

**作用面**：L5 新增出口、L4 全量改造（会话化、状态机）、L1 部分（可加多供应商路由）。

**问题**：需要同时维护前后端两套技术栈；且项目已主动移除过工作台。

### 方案 D · CLI 分层加固（不引入协议，只做工程内聚）

**思路**：保持单机 CLI，做四件小事：
1. 把 `chat()` 抽成 provider 接口（`ai.mjs:37`），为将来换端点/SDK 留缝；
2. 给 `shared/capabilities/*.json` 加 JSON Schema 校验（当前 `readCapability` 只做 `JSON.parse`，`ai.mjs:24`，配置写错要到运行时才炸）；
3. 给 `detectVerdict` / `sanitizeShellSubmission` 等纯函数补单测（当前 22 项，`README.md:116`）；
4. 加结构化 trace（`logger.mjs` 已 2.9 KB，可扩展为 JSONL）。

**作用面**：L1/L2/L4 部分，L3/L5 不动。

### 方案 E · 浏览器扩展（参考项，不推荐）

**思路**：MV3 扩展注入页面，绕开 CDP 调试端口的一切限制（`README.md:249` 已知边界第一条）。

**否决理由**：MV3 service worker 有生命周期回收，且扩展无法向页面内的 xterm 实例做真实键盘事件序列——命令行题（数据库运维题的半壁江山）直接失效。用"绕开一个约束"换"砍掉一整类题型"，不划算。

---

## 四、四维度对比评估

评分说明：**5 分制，统一"越高越优"**。"实现难度"与"开发维护成本"已做极性反转（难度越低、成本越低，得分越高）。

### 4.1 总表

| 维度 | A · MCP Server 化 | B · MCP Client 化 | C · HTTP API + UI | D · CLI 分层加固 |
|---|---|---|---|---|
| **可行性** | **4.0** — 官方 SDK 成熟，`solveOnce` 现成切分点 | **2.0** — 范式不匹配，能力覆盖不足 | **4.0** — 技术成熟，但已决策移除过 | **5.0** — 零新增依赖，随时可做 |
| **实现难度**（易=高分） | **3.0** — 需重构浏览器生命周期 + 工具粒度切分 | **1.5** — 等于重写 L3，且拿不回原能力 | **2.5** — 前后端两套 + 会话状态机 | **4.5** — 局部重构，低风险 |
| **系统适配性** | **3.5** — 适配宿主驱动；一键 bat 场景退化 | **2.0** — 通则不适配特例（Monaco/xterm） | **4.5** — 任意客户端可调，最通用 | **2.5** — 仅本机单人使用 |
| **开发维护成本**（低=高分） | **3.0** — 增量友好，但协议演进有长期成本 | **3.5** — 外部维护，但上游黑盒风险高 | **2.0** — 长期双栈维护 | **4.5** — 维护面最小 |
| **复用存量资产** | **4.5** | **1.0** | **3.0** | **5.0** |
| **综合（等权）** | **3.6** | **2.0** | **3.2** | **4.3** |

### 4.2 逐维度分析

#### 维度一：可行性

- **A 可行性高但非零风险**。MCP 官方提供 TypeScript SDK，stdio transport 与现有"单进程 CLI"模型天然同构（`cli.mjs` 就是进程级入口）。真正的风险不在 SDK，而在**浏览器生命周期语义的变化**：MCP server 常驻，而当前 `loop.mjs:630`、`cli.mjs:37` 都在 finally 里关闭浏览器。这是"进程模型"与"连接模型"的不匹配，必须在改造中显式解决。
- **B 可行性低**。问题不是"能不能连上 `playwright-mcp`"，而是连上之后**能力是净减少的**。`playwright-mcp` 不提供 Monaco `model.setValue` 语义，也不提供 xterm 逐字符键入。可行性评估不能只看"接口能通"。
- **C 可行性高**。技术栈全部成熟，无未知项。
- **D 可行性最高**。四件事全部是局部改动，且不改变任何对外行为。

#### 维度二：实现难度

按改造工作量从大到小：**B > C ≈ A > D**。

- **A 的三个真实工作量**（非 SDK 接入本身）：
  1. 浏览器生命周期重构：抽出 `ensureBrowser()` 单例，把 `close` 从 per-loop 移到 server shutdown。涉及 `loop.mjs` 四个导出函数。
  2. 工具粒度设计：`perceive.mjs`（40 KB）的输出结构对 LLM 而言过大，需裁剪为 schema 化的精简 payload。粒度太粗（只暴露 `solve_once`）则宿主失去控制权；太细则宿主需要自己实现反思编排——而反思逻辑恰恰是本项目的核心价值。**建议首版只暴露粗+中两档**。
  3. MCP Prompts 原语迁移：把 7 个能力 JSON 注册为 prompts，维持单一数据源约束。
- **B 难度最高**，因为它是"重写"而非"改造"。
- **D 难度最低**，但注意：D 的第 1 项（provider 抽象）会触及 `ai.mjs:37` 这个被 8 个上层函数依赖的入口，需要保持签名兼容。

#### 维度三：系统适配性

这一维度必须区分两种使用形态：

| 使用形态 | A | B | C | D |
|---|---|---|---|---|
| 双击 bat 一键跑（当前主形态） | 退化（需宿主） | 持平 | 持平 | 最适配 |
| 被其他 Agent 编排 | **最适配** | 一般 | 适配 | 不适配 |
| 远程 / 多人 | 不适配 | 不适配 | **最适配** | 不适配 |

**必须指出一个常被忽略的事实**：CDP 的硬约束（调试端口须在浏览器启动时带上、Edge 136+ 禁默认目录）在 **A/B/C/D 四个方案中全部依然存在**——它们都建立在"接管一个已带调试端口的浏览器"之上。唯一能绕开该约束的是方案 E，而 E 又砍掉了终端能力。**换言之：这是本题的架构级约束，不存在"换个集成方式就消失"的路径。**

#### 维度四：开发维护成本

- **A 的隐藏长期成本**：MCP 协议仍处快速演进期（规范已有多轮修订），SDK 与客户端实现都在变。一旦宿主的 MCP 版本与 server 不匹配，会出现"能连上但工具不可见"类的静默失败。这是协议化的固有代价，不是实现缺陷。
- **B 的成本结构最危险**：表面成本低（外部维护），实际是**把关键路径托付给一个与本题需求不匹配的上游**。上游一次重构就能击穿你的能力假设。
- **C 是长期成本最高项**：前后端双栈 + 会话状态 + 部署形态，全部要持续投入。
- **D 成本最低**。唯一"成本"是能力上限也低——它不会带来任何新的集成可能性。

---

## 五、优劣总结

| 方案 | 最大优点 | 最大缺点 | 一句话判断 |
|---|---|---|---|
| **A · MCP Server 化** | 纯增量、拿技术标签、复用全部存量资产 | 需重构浏览器生命周期；协议演进有长期跟随成本 | **最值得做的增量** |
| **B · MCP Client 化** | 表面减少自研代码 | 能力净减少，丢失 Monaco/xterm 两项不可替代资产 | **不要做** |
| **C · HTTP API + UI** | 适配性最广，可远程可多人 | 双栈维护；UI 部分已被主动移除过 | **只做 API、不做 UI，且非当前优先级** |
| **D · CLI 分层加固** | 零风险、零新增依赖、立即可做 | 不带来新的集成能力 | **应该先做，作为底座** |
| E · 浏览器扩展 | 绕开 CDP 端口约束 | 砍掉命令行题能力 | 不划算 |

---

## 六、分场景推荐建议

### 场景一：自用稳定优先

**执行 D，暂不碰 A。**

理由：当前链路已在真实 EduCoder 站点完成代码题端到端联调（`README.md:252`），任何重构都会引入回归风险。D 的四项改动全部不改变对外行为。其中**优先级最高的是第 2 项——为 `capabilities/*.json` 加 Schema 校验**：当前 `readCapability`（`ai.mjs:24`）只有裸 `JSON.parse`，配置写错要到运行时才暴露，而 prompt 是 AI 行为的唯一入口，值得前置校验。

### 场景二：求职作品集加分

**执行 A（作为增量层）+ D 的可观测项。**

理由（针对面试可讲性）：
1. **改造面小是可讲的优势，不是劣势**。可清楚陈述："MCP 化只新增 1 个文件，未破坏已通过端到端验证的 CLI 链路"——这体现的是对**回归风险的判断力**，比"重写了一遍"更有说服力。
2. **MCP 是本岗位的高频技术标签**，且你在实现中会真实踩到"常驻进程 vs 有状态连接"的问题，这是一个能延伸讨论的架构话题。
3. **可量化**：改造前后工具调用延迟、`solveOnce` 复用率、浏览器启停次数（改造后应从"每命令 1 次"降为"每会话 1 次"）。这个数字本身就是面试素材。
4. 若时间允许，**C 的 API 部分（不含 UI）可作为第二增量**，用于展示"协议无关的能力外露"设计意识。

### 场景三：纯技术可行性探讨

**A 与 C 的取舍是唯一有真正讨论价值的命题**，分野在于：

- 二者都要在 L5 新增出口，但 **A 把"谁来做编排"让渡给宿主，C 自己保留编排**。
- 本项目的核心资产恰恰是**编排与领域适配**（反思循环 + 平台机制破译），而**不是通用浏览器操作**。因此：**把编排让渡出去（A）等于让渡核心价值；自己保留编排（C）则符合资产分布**。
- 但反过来看：A 的"让渡"是可控的——只要工具粒度切到"中档"（暴露 per-perceive/submit 而保留反思在服务端），就能既被宿主驱动、又不丢编排。
- **这解释了为什么 A 的胜负手在"工具粒度设计"而不在"SDK 接入"。**

B 作为技术探讨的价值在于**反面案例**：可以完整论证"为什么通用浏览器 MCP 工具不能替代领域专用执行层"——这个话题在面试中比正面案例更能体现判断力。

---

## 七、方案 A 的最小落地路径（若采纳）

严格遵循最小侵入，五个改造点：

| # | 改造点 | 涉及文件 | 风险 |
|---|---|---|---|
| 1 | 新增 `agent/src/mcp-server.mjs`，`McpServer` + `StdioServerTransport` | 新增 1 文件 + 1 依赖 | 低 |
| 2 | **抽出 `ensureBrowser()` 单例**，把 `close` 从 per-loop 移出 | `loop.mjs:630` 及四个 loop 函数、`cli.mjs:37` | **中（唯一侵入点）** |
| 3 | 工具粒度切分：粗 `solve_current_task()`（复用 `loop.mjs:184 solveOnce`）+ 中 `perceive_page()` / `submit_answer()` / `read_eval_result()` | 新增 handler | 低 |
| 4 | 7 个 capability JSON 注册为 MCP Prompts 原语 | 读 `config.mjs:145` capabilitiesDir | 低 |
| 5 | 复用 `config.mjs:14-31` 的 `loadEnv` 与 `.env.local` | 不改 | 无 |

**首版建议范围**：只做 1 + 2 + 3（粗+中档），暂不做 4。理由：先验证"常驻进程 + 有状态浏览器连接"这个最不确定的组合，再考虑 Prompt 原语迁移。

**回归验证要求**（沿用项目既有纪律）：
```bash
cd agent
npm test          # 22 项单测必须全绿
npm run lint      # ESLint 零问题
npm run probe     # 确认识别逻辑未被破坏
```
并在改造前后各跑一次 `npm run once` 做端到端对照。

---

## 八、风险与边界声明

1. **评分主观性**：四维度分数是对代码事实的加权判断，非测量值。权重可调——若把"复用存量资产"设为最高权重，D（5.0）与 A（4.5）差距会进一步拉大；若把"系统适配性"设为最高，C（4.5）反超。
2. **未验证项**：本报告未实测 `@modelcontextprotocol/sdk` 的实际接入；A 的"实现难度 3.0"是基于代码结构推断（`loop.mjs` 与 `cli.mjs` 的浏览器生命周期写法确已核实），不是实测结论。
3. **B 的评估可能随上游变化**：若 `playwright-mcp` 未来提供 Monaco API 级别的编辑器控制与终端键盘事件注入，B 的可行性需重新评估。基于当前公开能力的判断不成立长期结论。
4. **协议版本风险**：MCP 规范仍在演进，本报告未逐一核对当前 SDK 版本与各宿主的支持矩阵，落地前需实测。
5. **本次分析为只读**：未修改仓库任何文件，报告仅输出至桌面。

---

*分析基于仓库 v1.1.1 代码快照，引用行号对应该快照。*
