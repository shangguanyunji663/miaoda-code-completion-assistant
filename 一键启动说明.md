# 一键启动 · 分支说明

> **分支**：`feat/4-e-oneclick-launcher`（基于 `feat/2-c-web-service` 1.6.21，agent 版本 **1.7.0**）
> **一句话**：同学从装环境到每天做题，只剩**双击两下**——第一次双击 `install.bat`，以后双击桌面「妙答」。
> **边界**：`agent/src` 与 `shared/` 零改动；`start-my-edge.bat` / `start-web.bat` / `start-watch.bat` / `start-lite.bat` / `start-course.bat` 等老入口全部原样保留。
> 姊妹方案：[`feat/5-f-electron-desktop`](https://github.com/shangguanyunji663/miaoda-code-completion-assistant/blob/feat/5-f-electron-desktop/%E6%A1%8C%E9%9D%A2%E7%89%88%E8%AF%B4%E6%98%8E.md)（Electron 安装版桌面程序）——两者互不依赖，怎么选见下表。

## 为什么有这个分支

同学们实际使用反馈的四个痛点，各自的解法都落在这一层（不改 AI 与执行逻辑）：

| 痛点 | 解法 |
|---|---|
| 启动步骤多、窗口多 | `start-miaoda.bat` 把「重启受控 Edge + 起网页工作台 + 打开页面」链进**一个窗口** |
| 首次安装配置麻烦 | `install.bat` 向导：自动装依赖、逐项问三件套写进 `.env.local`、建桌面快捷方式 |
| 出错不会排查 | 每一步失败都给**人话指引**，并指向 [`agent/docs/TROUBLESHOOTING.md`](agent/docs/TROUBLESHOOTING.md) 对应条目 |
| 日常误操作 | 调试端口已活就**复用浏览器不重启**；工作台端口被占不抢端口，直接开页面 |

## 首次安装（只一次，幂等可重跑）

双击 `agent/install.bat`，向导（`agent/scripts/setup.mjs`）做四步：

1. **Node.js 检查**：< 18 给出安装指引后退出；
2. **依赖安装**：`node_modules` 缺失才 `npm install`（唯一运行时依赖 playwright-core）；
3. **AI 三件套**：`.env.local` 缺失则从 `.env.example` 复制；`AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL` 任一为空且终端可交互时**逐项询问并写回**。
   - **密钥统一提供**：分发前把填好的 `.env.local` 放进包里，向导自动整段跳过；
   - **每人自己的密钥**：向导负责把三件套写进文件，不用记事本手改；
   - 终端不可交互（被脚本带管道调用）时只报告缺什么、不卡死；
4. **桌面快捷方式**：PowerShell 解析桌面（兼容 OneDrive 重定向）生成「妙答」.lnk；PowerShell 不可用退化为桌面 bat。

重复运行安全——已完成的步骤自动跳过。`--dry-run` 参数全程演练、不写任何东西。

## 每天做题：双击桌面「妙答」

`start-miaoda.bat` → `agent/scripts/launch-all.mjs`：

1. **受控浏览器**：调试端口（默认 9333）已活 → 直接复用，**不重启你的 Edge**（与旧流程唯一差异：日常双击不再折腾浏览器）；不在 → 复用与 `start-my-edge.bat` 完全相同的 `launchMyEdge()` 重启；
2. **网页工作台**：同进程运行 `src/web-server.mjs`（与 `start-web.bat` 完全同一入口）；
3. **自动打开** `http://127.0.0.1:8787`（`.env.local` 配了 `WEB_PORT` 则跟随）。

做题动作与以前完全一致：在受控 Edge 登录平台、打开题目页 → 回到网页点「探测页面 / 解当前题」。停止 = 黑窗口 `Ctrl+C` 或关窗。

## 三种用法怎么选

| 用法 | 适合 | 入口 |
|---|---|---|
| **本分支（推荐）** | Windows + Edge，想少点几下 | `install.bat` → 桌面「妙答」 |
| 经典三步 | 想理解每一步、或需要 watch/lite 模式 | `start-my-edge.bat` → `start-watch.bat` 等（全部保留） |
| [桌面版分支](https://github.com/shangguanyunji663/miaoda-code-completion-assistant/blob/feat/5-f-electron-desktop/%E6%A1%8C%E9%9D%A2%E7%89%88%E8%AF%B4%E6%98%8E.md) | 想要安装版程序、托盘常驻、无黑窗口 | `feat/5-f-electron-desktop` 的安装包 |

## 出错速查

| 症状 | 处置 |
|---|---|
| `install.bat` 提示 Node 未找到 | 先装 [Node.js LTS](https://nodejs.org)（≥18）再重跑 |
| npm install 报错 | 网络/换源问题，手动在 agent 目录执行 `npm install` 看完整报错 |
| 受控浏览器起不来 | 单独跑 `start-my-edge.bat` 复现；进程合并类问题见 TROUBLESHOOTING **E-4**，端口保留区间见 **E-1** |
| 提示工作台端口被占用 | 上次的工作台还在——直接用它打开的页面即可；真被别的程序占了在 `.env.local` 加 `WEB_PORT=8788` |
| 解题相关报错 | 与一键启动无关，按 [`agent/README.md`](agent/README.md) 与 TROUBLESHOOTING 代码级条目排查 |

## 验证边界（如实标注）

- `npm test` 218/218、`node --check` 通过；向导 `--dry-run` 实测四步（并借它修掉了 agentDir 解析 bug）。
- **未真机执行**：受控浏览器重启链路（`launchMyEdge` 会关闭运行中的 Edge，属破坏性动作）；「端口已复用」「PowerShell 不可用兜底」分支仅代码审查。分发前建议找一台干净 Windows 机器完整走一遍 `install.bat` → 桌面「妙答」。

详见 [`agent/CHANGELOG.md`](agent/CHANGELOG.md) 的 [1.7.0] 条目。
