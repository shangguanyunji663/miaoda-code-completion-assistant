# 本地密钥（含 API key）——切勿提交
.env.local

# 浏览器独立 profile（含登录态 Cookie）——切勿提交
.browser-profile/

# 页面结构快照
dumps/

node_modules/
# 注意：package-lock.json 必须提交——根 .gitignore 亦按此约定（依赖版本可复现）。
# 此处曾误将其忽略，与根目录说明冲突，1.1.0 修正。
