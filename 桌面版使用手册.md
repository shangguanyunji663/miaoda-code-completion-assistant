# 妙答桌面版 · 使用手册

> **给谁看**：拿到妙答桌面版（安装版 / 便携版）要上手使用的同学，**不需要会编程**。
> **适用版本**：桌面包 0.2.0（界面已按本手册描述的样子更新）。想对比"一键 bat 版（不装软件）"看《[桌面版说明.md](桌面版说明.md)》。
>
> 一句话记住这个软件：**妙答是"替你在评测网站做题"的自动化工具。** 你在浏览器里打开某道题，点一个按钮，它就自动读懂题目、写答案、提交评测；做不对会自己反思再试几轮。下面按"装 → 首次配置 → 每天怎么用 → 收不了尾怎么办"的顺序讲。

---

## 0. 先看清楚你要准备什么

| 你需要有的东西 | 说明 |
|---|---|
| 妙答桌面版 | 安装版（一个 `.exe` 安装器）或便携版（`Miaoda-portable-x.x.x.exe` 单个文件） |
| **AI 三件套** | **端点地址 / 密钥 / 模型名**，由发给你工具的人提供。没有这三样填不了首次配置。 |
| 评测平台账号 | 你自己能登录那个写代码做题的网站（首次会自动帮你登好）。 |

三件套长这样、从哪来（对着填就不会错）：

| 项目 | 长什么样 | 从哪拿 |
|---|---|---|
| **端点地址** | 一个网址，一般以 `/v1` 结尾，如 `https://xxx.xxx/v1` | 发你工具的人 |
| **密钥** | 一长串，常以 `sk-` 开头，**等同密码** | 发你工具的人 |
| **模型名** | 一个英文词组，如 `deepseek-chat` | 发你工具的人（不确定就照他给的填） |

---

## 1. 安装（只做一次）

**安装版**
1. 双击 `.exe` 安装器，一路「下一步」（默认装到当前用户目录，不需要管理员）。
2. 装完桌面 / 开始菜单会出现「妙答」图标。

**便携版**
1. 把 `Miaoda-portable-*.exe` 放到任意你想放的位置（U 盘、桌面都行）。
2. 双击即运行，**不用安装**；删掉这个文件就等于卸载（配置见 §4，存在系统里不在这个文件里）。

> 两者区别只是"要不要安装器"，**用起来完全一样**。下面都以"双击妙答"指代启动。

---

## 2. 首次配置（只做一次）

1. 第一次双击「妙答」，会先弹出一个 **首次配置** 窗口（深色、三个输入框）。
2. 把 §0 的三件套填进三个框，点「**保存并启动**」。
   - 某格不知道填什么 → 先问发你工具的人，**不要瞎猜**（密钥尤其要一字不差）。
   - 窗口底下有一行小字：三件套**只存在你自己电脑**上，不外传、不上传。

保存成功后：

3. 软件会**自动准备一个受控浏览器**（帮你打开 / 重启 Edge 并带上"能被妙答操控"的开关）。
   - **这一步会先把你当前开着的 Edge 关掉**——所以**首次启动前，先把 Edge 里没保存的标签页存好**。
   - 如果等不到就绪，会弹个气泡提示"受控浏览器 45 秒内未就绪"，你在主界面点一次「探测页面」通常就能续上。
4. 主窗口自动打开**网页工作台**（深色、左侧一排按钮、右侧结果与日志）。到这就完成了首次启动。

> 之后每天双击妙答就**不会再弹配置窗口**，直接进主界面。

---

## 3. 每天怎么用（核心，照着点）

主界面左侧有三个按钮，你 90% 的操作就是点它们：

```
[ 探测页面（只读） ]      ← 先点这个，确认妙答看到的是"哪道题"
[ ▶ 解当前题 ]           ← 确认后点这个，它才开始写、提交
[ ■ 停止做题 ]           ← 一直不过、或你不想等了就点它
```

### 完整流程（做一道题）

**第 1 步：打开题。** 在妙答自动帮你准备好的那个 Edge 里，登录评测平台，**点进你要做的那一题**，停在"能看到题目 + 编辑器/终端"的页面上。

> 关键：**同一时间只保留一个题目标签页**。开好几个题目标签会让妙答挑花眼（探测结果里如果冒出"多个候选页"，就是你现在开太多题了，关掉多余的再探一次）。

**第 2 步：探测（只读、不答题、不提交）。** 点「**探测页面（只读）**」。右侧"执行结果"会显示它认到的目标页 URL、题型、题干开头几行——**看一眼，确认是你要做的那题**。认错了就回到第 1 步。

**第 3 步：解题（会真实提交评测）。** 点「**▶ 解当前题**」，会弹一个确认框（"将真实点击『评测』按钮提交本题"），点「**确定，开始解题**」。
   - 左侧"运行中：……"会滚动显示它走到哪一步；右侧"运行日志"逐条打印过程。
   - 一题可能要**几十秒到几分钟**（中间会失败自动反思重试，默认最多 10 轮），**耐心等，别关窗口**。

**第 4 步：看结果。** 右侧"执行结果"给出三态之一：
   - **绿色 ✓ 评测通过** —— 这题成了。想继续做下一题 → 浏览器切到下一题，重走第 2、3 步。
   - **红色 ✕ 未通过** —— 自动重试到额度仍不对。往下有"评测输出"（预期 vs 实际），看不懂就把这段**连同日志**发给发你工具的人。
   - **黄色 ■ 已手动停止** —— 你自己按了停止，不是失败，想接着做再点一次「解当前题」即可。

**随时止损：** 发现题目本身有问题、或不想再烧时间了，点「**■ 停止做题**」——它**不会**再提交下一次评测、也**不会**再发起下一次 AI 调用；停在当前这一步结束就干净收尾。

**换模型（进阶）：** 左侧"模型名"输入框随时改、回车保存，**立即生效、不用重启**。这也是**填错模型名**时最简单的补救（见 §4）。

---

## 4. 收不了尾 / 用着用着出岔子

| 现象 | 怎么办 |
|---|---|
| 弹"受控浏览器 45 秒内未就绪" | 主界面点一次「探测页面」；仍不行就确认受控 Edge 没被手动关掉、没有别的程序占着调试端口。 |
| 探测出来**不是**我要的那题 / 冒出"多个候选页" | 关掉多余的题目标签，只留一个，再探。 |
| 解了一堆轮还不过 | 点「停止做题」止损；把右侧"评测输出 + 运行日志"发给发你工具的人，**别自己手改答案**。 |
| **模型名**填错了 | 不用动文件：左侧"模型名"输入框改对、回车保存即可（热生效）。 |
| **端点 / 密钥**填错了（报错连不上、或说没权限） | 删掉 `.env.local` 再启动妙答，会**重新弹配置窗口**重填。文件位置：安装版在 `%LOCALAPPDATA%\Programs\Miaoda\resources\agent\.env.local`（在地址栏粘贴 `%LOCALAPPDATA%\Programs\Miaoda\resources\agent` 回车可直达该文件夹）；便携版运行时会解压到系统临时目录，直接找该目录下的 `agent\.env.local`。**不确定就找发你工具的人要一份预填好的包**最省事。 |
| 想彻底关掉妙答 | 关窗只是**藏到托盘**（右下角那个小图标），**不是退出**；要真退出，右键托盘图标 →「退出」。 |
| 换了台电脑 / 重装系统 | 三件套**不会自动跟过去**，新机器要重新走一次 §2 首次配置。 |

**日志在哪**（排障用）：

- 桌面壳日志：`%APPDATA%\miaoda-desktop\agent-web.log`（agent 全部输出）
- agent 运行日志：`agent\logs\agent-<日期>.log`（和命令行模式同一份）

---

## 5. 安全与隐私（知道即可）

- **三件套只在你本机**：配置写在 `agent\.env.local`，**不会**被打进安装包、**不会**上传到任何服务器；重装/换机它不跟随。
- **工作台只在本机可达**：网页工作台只监听 `127.0.0.1`（本机回环地址），别人从你局域网访问不到。
- **不会偷偷改你的 Edge 设置**：受控浏览器是"带调试开关"开的，用完（托盘退出）即结束，不残留。

---

> **相关文档**：分支定位、与一键 bat 版怎么选、构建/分发细节、验证边界 → 《[桌面版说明.md](桌面版说明.md)》；变更历史 → `desktop/CHANGELOG.md`；排障深入（识别/解题类问题）→ `agent/docs/TROUBLESHOOTING.md`。
