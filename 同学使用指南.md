# 同学使用指南（傻瓜版）

> 给第一次接触本项目的同学：照着做就行，每一步都写了"你应该看到什么"。
> 本指南面向 **Windows + Edge**（项目主要验证环境）；Mac/Linux 用户参考 `agent/README.md`。
> 用完这篇文章，你只需要记住一句话：**双击两个 bat，剩下的它自己做**。

---

## 一、这是什么（30 秒版）

一个跑在你自己电脑上的"自动做题助手"：它接管**你自己已经登录好的**浏览器，读题目 → 让 AI 写答案 → 自动填进去 → 点"评测" → 没过就自动反思重试。

三件事必须先说清楚：

1. **它操作的是你的真实账号、真实提交**——是否违反你们平台的使用条款，请自己判断；
2. **它不破解、不绕过登录**——你必须在受控浏览器里自己登录；
3. **它需要你有一个 AI 密钥**——做题是 AI 在做，密钥的额度是你自己的。

---

## 二、开始前，准备三样东西

| # | 东西 | 说明 |
|---|---|---|
| 1 | Windows 电脑 + Edge 浏览器 | 你平时用的 Edge 就行 |
| 2 | Node.js（第 1 步带你装） | 版本 ≥18 即可，20 / 22 / 24 都行 |
| 3 | AI 密钥三件套 | 找发给你这份工具的人要，或自己注册一个"OpenAI 兼容"端点 |

**AI 密钥三件套**是一段配置里的三个值，拿到后先记在记事本里：

```
AI_BASE_URL=https://xxx.xxx/v1     ← 端点地址（一般以 /v1 结尾）
AI_API_KEY=sk-xxxxxxxx             ← 密钥
AI_MODEL=xxxxxxxx                  ← 模型名
```

---

## 三、七步跑起来

### 第 1 步 · 安装 Node.js（只做一次）

1. 打开官网 <https://nodejs.org>，下载 **LTS 版本**；
2. 双击安装包，一路"下一步"（默认会自动加入 PATH）；
3. 验证：按 `Win + R`，输入 `cmd` 回车，在黑窗口里输入：

```
node --version
```

- ✅ 显示 `v18.`、`v20.`、`v22.`、`v24.` 之类的版本号 → 成功；
- ❌ 显示"不是内部或外部命令" → 重新安装 Node，安装时勾选 **Add to PATH**。

### 第 2 步 · 拿到项目并安装依赖（只做一次）

1. 把项目文件夹放到电脑上（解压 zip，或 `git clone`），比如放到 `D:\miaoda`；
2. 打开该文件夹，**再打开里面的 `agent` 文件夹**；
3. 教你一个免学命令行的技巧：**在文件夹的地址栏里输入 `cmd` 然后回车**——会直接在当前文件夹打开命令行窗口；
4. 在这个窗口输入：

```
npm install
```

- ✅ 滚动一阵后结束，**没有红色 `ERR!`** 即成功（黄色的 warn 不用管），首次约 1–2 分钟；
- ❌ 有红色 ERR → 截图发给发你工具的人。

### 第 3 步 · 配置 AI 密钥（只做一次）

1. 还是在 `agent` 文件夹的命令行窗口，输入：

```
copy .env.example .env.local
```

2. 用**记事本**打开刚生成的 `.env.local`，找到下面三行，把开头 `#` 号删掉并填入你的三件套：

```
AI_BASE_URL=https://xxx.xxx/v1
AI_API_KEY=sk-xxxxxxxx
AI_MODEL=xxxxxxxx
```

3. 保存并关闭。

> ⚠️ `.env.local` 里是你的密钥：**不要发给任何人、不要上传到网上**。

### 第 4 步 · 启动"受控浏览器"

进入 `agent` 文件夹，**双击 `start-my-edge.bat`**。

- 它会先**关闭你正在运行的 Edge**（3 秒倒计时，重新打开后标签页都能恢复）——这是必须的，正常现象，不是出 bug；
- 之后自动弹出的 Edge 就是"带调试端口的受控浏览器"，你的账号、收藏夹全都在；
- 窗口里会显示一行端点（如 `http://127.0.0.1:9333`），不用管它，最小化即可。

### 第 5 步 · 登录评测网站，打开一道题目页

在第 4 步弹出的这个 Edge 里：

1. 登录你们的评测平台；
2. 点进**任意一道题**，停在能看到题目和代码框的页面。

### 第 6 步 · 先体检一次（只做一次）

回到第 2 步的命令行窗口（地址栏 cmd 那个），输入：

```
npm run probe
```

- ✅ 看到类似 `题型: 代码`、`编辑器: Monaco`、题干前 300 字的内容 → 一切正常；
- ❌ 对照排查：

| 报错关键字 | 原因 | 怎么办 |
|---|---|---|
| `缺少 AI_BASE_URL` | 第 3 步没做/没保存 | 回第 3 步 |
| `无法连接浏览器调试端口` | 受控浏览器没开 | 回第 4 步 |
| `页面渲染为空` | 评测网站没登录 | 回第 5 步 |
| 其他红色报错 | — | 截图（连同窗口内容）发给发你工具的人 |

### 第 7 步 · 开始自动做题

**双击 `start-watch.bat`**。它会一直挂着：

- 你在受控浏览器里**切到哪道题，它就自动做哪道题**；
- 它**不会**替你点"下一关"——做完一题，你自己翻到下一题，它继续；
- 想**换思路重做**当前题：双击 `start-lite.bat`，刷新页面（F5）即重做；
- 停止：在那个黑窗口按 `Ctrl+C`，或直接关窗口。

---

## 四、装好之后的日常，只有两步

1. 双击 `start-my-edge.bat`（开受控浏览器，登录评测站）；
2. 双击 `start-watch.bat`（开始自动做题）。

用完关掉 watch 的窗口即可。

---

## 五、常见问题（按症状查）

| 症状 | 原因 | 处理 |
|---|---|---|
| `npm 不是内部或外部命令` | Node 没装好 | 重装 Node，勾选 Add to PATH |
| `缺少 AI_BASE_URL 配置` | 密钥没配好 | 回第 3 步 |
| `无法连接浏览器调试端口` | 受控浏览器没启动 | 双击 `start-my-edge.bat`；或双击 `start-browser.bat` 用独立浏览器 |
| `页面渲染为空` | 评测站没登录 | 在受控浏览器里登录一次 |
| AI 反复重试、最后失败 | 密钥错误 / 额度用完 / 模型名写错 | 检查三件套；找发你工具的人确认 |
| 题目识别不对（题型/编辑器是"未识别"） | 平台页面结构特殊 | 双击 `start-course.bat` 旁的 `npm run dump` 导出快照，发给维护者 |
| 想看它刚才干了什么 | — | 打开 `agent/logs/` 里当天日期的日志文件 |
| 弹窗"通过本关"但没继续 | 正常——它不替你翻页 | 自己点"下一关" |

---

## 六、安全与账号，务必读一遍

- 它**不碰登录环节**，只操作你自己登录好的页面；
- **自动提交是真实提交**，作用于你的真实账号与额度，平台条款请自行评估；
- `.env.local` 是你的密钥文件，等同于密码：**不外发、不上传**（项目已默认排除在 git 之外）；
- 项目仅供学习与技术交流。

---

## 七、想玩进阶？

当前 master 是最稳定的"自动做题"本体。项目还有三个功能分支（给 AI 编程助手当工具用的 MCP 版、网页点按钮版、能力配置加固版），进阶玩法见 `agent/README.md` 与各分支的同名指南。
