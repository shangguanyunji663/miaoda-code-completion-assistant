# 桌面版 · 分支说明

> **分支**：`feat/5-f-electron-desktop`（基于 `feat/2-c-web-service` 1.6.21；桌面包版本 **0.1.0**，agent 保持 1.6.21 不动）
> **一句话**：把「受控浏览器 + 网页工作台」装成一个 Windows 桌面程序——有图标、托盘常驻、没有黑窗口。
> **边界**：`agent/src` 与 `shared/` 零改动。桌面版只是一个**壳**：用 Electron 自带 Node 以子进程方式运行 agent 的**既有入口**（`web-server.mjs` 与 `cli.mjs my-edge`），不复制、不改写任何 agent 逻辑。
> 姊妹方案：[`feat/4-e-oneclick-launcher`](https://github.com/shangguanyunji663/miaoda-code-completion-assistant/blob/feat/4-e-oneclick-launcher/%E4%B8%80%E9%94%AE%E5%90%AF%E5%8A%A8%E8%AF%B4%E6%98%8E.md)（一键 bat，agent 1.7.0）——两者互不依赖，怎么选见下表。

## 同学视角

**首次（只一次）**：

1. 拿到安装包（NSIS 安装版）或便携单文件 exe，打开；
2. 若 `agent/.env.local` 不存在，会弹出**首次配置窗口**：填 AI 三件套（端点 / 密钥 / 模型名）→ 保存。三件套只存在本机，**不会被打进分发包**（打包白名单不含 `.env.local`，分发前已核验）；分发方预填了则此窗口自动跳过；
3. 主窗口自动打开网页工作台，同时自动准备受控 Edge。

**每天**：双击「妙答」→ 在受控 Edge 登录平台、打开题目页 → 在主窗口点「探测页面 / 解当前题」。

**关闭行为**：点 × = **隐藏到托盘**继续运行（首次有气泡提示），受控浏览器与工作台保持可用；托盘图标右键 →「退出」才真正结束。这与旧版「黑窗口必须保持开着」的心智一致，但不再占任务栏。

## 和一键 bat（feat/4-e）怎么选

| | 一键 bat（agent 1.7.0） | 桌面版（本分支） |
|---|---|---|
| 形态 | 1 个黑窗口 + 1 个网页 | 安装版程序 / 便携 exe，无黑窗口 |
| 依赖 | 零新增（纯 Node 脚本） | Electron（仅桌面包的开发依赖，不影响 agent） |
| 分发 | 仓库分支直接用 | 需先构建（见下） |
| 托盘常驻 | 无（关窗即停） | 有（关窗隐藏，托盘退出） |
| 适合 | 先跑起来 / 机器受限 | 想要"正规软件"体验 |

两条分支可以共存于同一台电脑（配置同为 `agent/.env.local`，端口复用逻辑相同：CDP 端口已活不重启 Edge、工作台端口被占不抢端口）。

## 构建与分发（给维护者）

```bash
cd desktop
npm install        # 一次性下载 Electron（约 100MB+）
npm run dist       # 产出 NSIS 安装版 + 便携单文件 → desktop/dist/
```

打包把 `agent/`（含 node_modules）与 `shared/` 经 `extraResources` 原样拷入资源目录；运行期写入的 `.env.local` 与日志保存在用户目录，换机不迁移。**完整构建参数、目录布局、Windows 符号链接特权坑的绕过方法**见 [`desktop/README.md`](desktop/README.md)。

## 日志与排障

- 桌面壳日志：`%APPDATA%/miaoda-desktop/agent-web.log`（agent 子进程的全部输出）；
- agent 运行日志：`agent/logs/agent-<日期>.log`（与 CLI 模式同一份）；
- 解题/识别类问题与 CLI 完全同源，按 [`agent/docs/TROUBLESHOOTING.md`](agent/docs/TROUBLESHOOTING.md) 排查。

## 验证边界（如实标注）

- **已实测**：`npm install` → `npm run dist` 全链路（产出 `Miaoda-portable-0.1.0.exe` 76MB，未签名属预期）；`resources/agent`（含 playwright-core）与 `resources/shared` 布局核验正确；**包内确认无 `.env.local`**；`electron --version`（v33.4.11）。
- **未实测**：Electron 主进程的完整运行链路（配置窗口 → 子进程 → 托盘）——`my-edge` 会关闭运行中的 Edge，属破坏性动作，开发机上未执行。首次真机运行请先保存好浏览器标签页。
- NSIS 安装版目标未单独构建（与便携版同一管线，仅产物形态差异）。

详见 [`desktop/README.md`](desktop/README.md)、桌面包变更日志 [`desktop/CHANGELOG.md`](desktop/CHANGELOG.md)；agent 侧变更见 [`agent/CHANGELOG.md`](agent/CHANGELOG.md)。
