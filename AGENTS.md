# AGENTS.md

面向在此仓库工作的开发代理（Agent）的项目说明。

## 项目性质

**浏览器自动解题 Agent**：通过 CDP 连接用户已登录的 Edge/Chrome，自动感知评测网页上的题目，AI 生成答案后自动作答、提交评测、失败反思重试。

项目主体是 `agent/`（Node + playwright-core；不下载浏览器，走 CDP 连接用户自己的浏览器）。
早期版本另有一个手动粘贴式的 React 工作台前端，已于 2026-09-09 移除，历史版本见 git 提交 `3cc19d3`。

## 核心模块（`agent/src/`）

- `config.mjs` — 配置层，读 `agent/.env.local`
- `ai.mjs` — OpenAI 兼容调用 + `detectVerdict()` 加固版成功判定；`classifyProblemIntent()` 意图路由（code / cmdline / mixed）；`spliceIntoTemplate()` 模板拼接（标记按序归位 + 完整代码直通 + 标记不成对直通 + 缩进保留）；chat 流式思考控制（硬闸/停滞检测/字数配额/首轮关思考）；prompt 读 `shared/capabilities/*.json`（首次读取自动触发全量预检）
- `capability-schema.mjs` — 能力 JSON 加载前校验（必填字段、prompt 占位符 ⊆ paramsSchema.properties、required 一致性），防"占位符拼写错误静默渲染为空串"
- `browser.mjs` — `connectOverCDP` + 四级挑页链选目标标签页（`pickTargetPageWithMeta`：TARGET_URL_HINT → TASK_URL_PATTERN 形状 → 内容级特征 → 首个非空白；URL 层多命中报错防解错页；CDP 下 visibilityState 不可用，见 TROUBLESHOOTING C-4/C-11）+ 连接前临时摘除代理环境变量
- `task-url.mjs` — 题目页 URL 识别共享小模块（`taskKey`/`isTaskUrl`，正则编译带缓存；watch/lite 与挑页链共用 `TASK_URL_PATTERN` 单旋钮）
- `browser-session.mjs` — 常驻进程的浏览器会话管理（懒连接 + 互斥串行 + 断线重连），供 web-server 复用
- `control.mjs` — 运行控制层：`StopRequested` + `beginRun`/`endRun` + `requestStop`/`checkStop`/`isStopping` + `onStop` 订阅（在途 AI 请求 abort）+ `stopState`（供 `/api/status` 展示运行阶段）。停止请求有**轮次隔离**（记住"发出时跑的是第几轮"，只对该轮生效），中断是**协作式**的——靠各层检查点抛异常，不取消 playwright 调用
- `web-server.mjs` — 网页工作台（零新增依赖 node:http + 单文件原生前端，仅绑 127.0.0.1）：`GET /`、`GET /api/status`（每次请求比对 `.env.local` 模型差异热生效；含运行态 `run`）、`POST /api/probe`（只读）、`POST /api/solve`（真实提交；被停止时返回 HTTP 200 + `stopped:true`）、`POST /api/stop`（手动打断在途解题，**刻意不走浏览器互斥队列**）、`GET /api/logs`（环形缓冲增量轮询）、`/api/config`（页面写回模型名到 `.env.local`）
- `launch-browser.mjs` — 启动带调试端口的浏览器，含 Windows 保留端口区间自动顺延
- `perceive.mjs` — 页面感知：编辑器探测、题干提取、题型分类（选择/填空按结构信号；代码/命令行/混合由 AI 按题干意图判定）、按钮枚举、课程列表卡片/板块收集、终端环境识别（`detectTerminalEnv`：bash/mongosh/mysql/redis/psql/neo4j；`waitForTerminalEnv` 轮询等**内容就绪**——`.xterm-screen` 可见 ≠ 提示符已打印，`describeTerminalWait` / `unknownEnvDesc` 把"无回显"与"提示符形态不认"分开写进日志与 prompt）、内容级判题（`looksLikeTaskPage`：强代码编辑器/评测面板标记/评测按钮，挑页链兜底用）
- `act.mjs` — 执行层：写入代码、勾选选项、点击评测、等待结果、翻页、课程导航（退出/返回/开始学习/跳转检测）、切换「命令行/代码文件」工作区标签、向 xterm 终端逐条键入命令、客户端可用性实测（`probeTerminalClients`）、shell 护栏清洗（`sanitizeShellSubmission`）。1.5.0 两条硬约束：① `waitEvalResult(page, ms, {codeUnchanged})` **只在提交文本与上次送进评测的相同时**才允许采信"面板与点击前一致"，且等待预算按面板「本关最大执行时间」动态抬高，未观测到变化就返回带 `STALE_EVAL_PREFIX` 的文本（`parsePlatformMaxSeconds` / `evalDeadlineAt` 为可单测纯函数）；② `clickByKeywords` 返回 `exists`，"按钮点不动"（上一轮评测仍在跑）与"页面没有按钮"分开记，`clickEval` 重扫窗口默认 150s 且每轮过 `checkStop`。1.6.9 起明细抓取不再按 innerText 位置切分：`COLLECT_TEST_SETS` 改 **DOM 文本锚定**——本平台把两个栏目标记**并排**渲染在标题行、两份正文整块挂在标题行的**下一个兄弟元素**（两个并列子元素），按位置切分会把预期段清空，并连带让「差异定位 + 容器格式反解」同时失效（结构不符回退旧切分；采集到预期段为空时 `log.warn` 点名抓取侧）。1.6.13 修**输入期报错的窗口计算**：`runTerminalCommands` 原先拿 `.xterm-rows` 的**行数**当游标（首次调用 `slice(0)` = 整屏、滚满后 `slice(行数)` 恒为空），于是上一轮遗留的报错冒充"命令 1 报错"、第 2 条起则从来没检出过东西（全日志 18 次报错 100% 落在命令 1）⇒ 改由零依赖纯函数 `newTerminalLines` **按内容求重叠**定位新增行，并额外处理"末行是光标行、会被就地改写而非平移"；键入前先取基线快照，日志带「本条窗口 N 行」。1.6.24 键入循环加**必败命令跳过**：某路径已由序列内前一条命令实测为不存在、且剩余命令里没有任何创建动作 ⇒ 不再键入（2026-09-25 真机：17 条里第 1 条 `find /opt` 就摊出 /opt 是空的、第 3~6 条 `ls` 全部 No such file，后面 9 条 `mongorestore` 引用同一批已判不存在的路径照样敲完）。只读取证（`silent`）不参与跳过——对不存在的目录敲 `find` 拿回报错正是取证目的。归因纪律同 py2-guard「宁漏不误报」：行级归因优先，**命令只有一个路径操作数**时才允许兜底，且兜底硬性要求"确实出现过不存在信号行"（否则 `find /opt -maxdepth 3 -type d` 正常打印一个 `/opt` 就会把后续整串判死——这条在自查回放里真翻过车）
- `cmd-evidence.mjs` — 回显失败判据 + 路径只读取证（1.6.20，零依赖纯函数）。`act.mjs` 原先只有一条"错误措辞"正则，而真机最强的一条线索 `don't know what to do with subdirectory "mongodb_1/test1", skipping...` + `done` **不带 error 字样、退出码 0、实际什么都没做**，从未进过反思材料（模型因此连猜三轮路径名，还编出"备份目录是空的"这种被同一次回显直接否证的结论）。现分两档：`explicit`（明确错误语法）与 `silent-noop`（工具自述跳过/不认识/无事可做），**两档只认通用英文错误与自述语法，不绑定 mongorestore/mongodump 等工具名**（mysql `source`、tar、自写脚本同形）；`skipping` 只认进行式是有意为之——`{skip:10}` 这类合法写法会出现在回显的命令本身里。另提供 `extractMissingPaths`（从"路径不存在/是目录/无法访问"的行里抠祖先目录，**具体目录 + 顶层两种粒度**：xterm 按列硬换行会把长路径劈成半截，只取前两段会得到根本不存在的根）与 `pathProbeCommands`（`find <根> -maxdepth N 2>&1 | head -80`，**上限 3 条、只用 ls/find**）；抠出的路径要打进真实 shell，故必须过绝对路径白名单（拒 `; \` $ 引号 重定向 括号 通配符 空白 非 ASCII`），与 format-probe 的键名白名单同一条纪律。执行编排在 `loop.mjs` 的 `probeMissingPaths`（混合题准备报错后 / 命令行分支评测失败后；放在输出指纹之后免污染比对基准；1.6.21 起敲探针前先轮询等 bash 提示符——终端可能停在 REPL 里，`find` 敲进 REPL 只会得到一条解析错误；`runTerminalCommands` 的 `silent` 用于不刷日志——取证 `find` 命中空目录的 No such file 是**证据**不是故障）。1.6.23 再加 `detectMissingPremise`（零依赖纯函数）：**"前置数据缺失"档**——题面引用的源数据目录（如 `/opt/mongodb*`）在**顶层目录**实测清单里**全部缺席**（顶层 `/opt` 为空目录或本身不存在）时，与"猜错路径名"分档：这不是命令形态问题，**任何改写都造不出数据**，命中即注入处置段（重做上一关备份 / 重置环境后按课程顺序重做，留"若题面要你创建这些数据则忽略"逃生口）。判据宁漏不误报——顶层根 + ≥2 个其下具体缺失根、且顶层 `find` 探针确已执行、且段中无 T 下任何路径行，三条全中才判，任一不满足 fail-open；`loop.mjs` 命令行分支 `premiseStreak` 命中即止损 `prerequisite-missing`（1.6.23 要求连续 2 轮；**1.6.24 起改为锁存计数 + 按 `taskCreatesOwnData` 分 1/2 轮**），课程模式 `solveBoard` 遇该原因直接结束本板块。1.6.24 把这条判据的输入换成题面：新增 `requiredPathsFromProblem`（**只认题干「编程要求」段**——按全文匹配会把相关知识里 `-o 指明到要导出的文件名` 误读成"题面要你自造数据"，且 `编程要求` 在页面文本里出现两次，须取最后一次）、`topLevelRoots`、`taskCreatesOwnData`（止损阈值分档）、`absentPathsFromEcho` + `isDeadCommand`（必败命令判定）；`detectMissingPremise` 加 **⓪ 号条件「参与判定的顶层必须来自题面」**——回显会被 xterm 按列劈行，`/opt/collection_1` 能劈成上行的 `/opt` + 下行的 `/collection_1`，于是"根"清单里混进**幽灵顶层目录**，而旧三条判据对幽灵根全部"成立"（幽灵根当然 find 不到，find 不到的命令还是我们自己生成的），据此会把一道 `/opt/collection_1` 其实有数据的可解题判死；`pathProbeCommands` 改**顶层根优先**（一条 `find /opt -maxdepth 4` 摊开整棵树，别把预算花在读不出东西的碎片上）
- `py2-guard.mjs` — 写入前本地语法守卫（1.5.0，零依赖纯函数）：`checkPython2Syntax(code)` 用「字符串/注释掩码 + 括号栈」检出 Python 2 下**必定 SyntaxError** 的 Python 3 形态（PEP 448 `f(*a, x)`、f-string、`:=`、类型注解、`def f(a, *, b)`、`nonlocal`、`yield from`、`async/await`、`raise X from Y`、`except*`、字面量内解包、无参 `super()`）。三条纪律：**宁漏不误报**（误报会让模型来回改本来正确的代码）、非 Python 提交整体跳过、自身异常 fail-open 绝不阻断主流程
- `requirement-contract.mjs` — 题面契约层（1.6.0，零依赖纯函数）：`extractRequirementContract` 从**当次题干**确定性切出「编程要求」条目 + 「测试说明」预期输出；`renderContractBlock` 作为**数据**注入 prompt（指令文字仍留在 capability JSON，遵守 prompt 单一数据源）；模型须在代码块前输出 `编号 | 题面原文摘录 | 实现行号 | 说明` 对齐表；`validateAlignment` 机器校验——**漏条目**与**摘录不是题干真子串**（幻觉引用）为打回级，行号越界只作提示（模板拼接会让行号偏移）。题干切不出该格式 → `present:false`，整层不启用（跨平台 fail-open）。1.6.2 再加 `findRedundantPrints`：**「编程要求」里没有任何"打印/输出"措辞时，Begin/End 里的 print 一律判为复制了评测程序的输出行**——真要求输出的题自动不触发。1.6.19 再修切条噪声：`现有 X 文件内容如下：` / `在右侧命令行进行操作：` / `在右侧代码行 Begin-End 中编辑，如下：` 这类**叙述行**（数据描述、操作位置提示）不再进要求清单——旧版把它们算成 12 条里的几条，模型永远给不出落点 ⇒ `missing_item` 一次报满 ⇒ 触发"同类问题打回一次即停止打回"的止损，**整个契约层被噪声关掉、真正漏答的那条反而没人拦**（2026-09-24 真机：7 条查询对 8 个标签烧了 3 轮）
- `format-probe.mjs` — 容器格式反解探针（1.6.8）：顺序类差异（`DICT_ORDER` 哈希键序 / `ORDER_ONLY` 的 set 序）是本项目唯一"本地算不出、模型必然猜不对、试错代价是一轮评测（≈120s）"的一类，而**容器里就有平台的 Python 2**，所以反思前借题目页「命令行」跑 `python -c` **纯计算**把答案算出来。`planFormatProbe` 折出待探项（键数 ≤8、单次 ≤2 组），`probeCommands` 生成命令，`parseProbeOutput` 只认 `DP <编号> <命中数> <首解序号>` / `SP <编号> <是否一致>` 标记行，`renderProbeFindings` 出注入反思的中文段，`unrankPermutation` 把首解序号还原成可照抄的写入序（与 `itertools.permutations` 同序，已用真机 rank 184 钉死）。两条设计约束值得记住：① **回显只回编号不回整条顺序**——xterm 按列硬换行会把键名劈开（取证时亲眼见过），逐行正则当场废掉，长文本改由 JS 反排；② **键名白名单 `/^[A-Za-z0-9_.-]+$/`**——这些串是从网页文本抠出来、要被打进真实 shell 的，不过滤等于开命令注入面（非 ASCII 另有独立原因：py2 的 `python -c` 里裸非 ASCII 直接 SyntaxError）。**无解时结论自动翻向**：0 种写入顺序能打印成预期 ⇒ "这不是顺序问题，是键集合问题"，指向评测脚本 `pop(x,"404")` 掉的那些字段。执行编排在 `loop.mjs` 的 `probeFormatOrder`（切标签 → **轮询等待 shell 提示符** → 跑 → **finally 切回「代码文件」**；放在输出指纹**之后**，否则探针文本会污染"改了等于没改"的比对基准）；开关 `FORMAT_PROBE`（默认开）。1.6.9 补两处真机教训与一条新闭环：① `waitForTerminal` 只保证 `.xterm-screen` **可见**、不保证内容就绪，评测刚结束时读一次会拿到空行而误判 `unknown`、静默放弃 ⇒ 改为 `waitForShellPrompt` 轮询（15s / 800ms），失败日志带末行原文；② 探针两处静默 `return` 补上原因（"任何一步不满足都放弃并记日志"此前并未遵守）；③ **「0 解 → 补字段」闭环**——`DICT_ORDER` 按可见键集合 0 解时说明字段集合与参考实现不同，而参考多写的字段只存在于评测脚本的 `X.pop("字段","404")` 里（题面「示意」是**图片**，文本层为空），于是只读 `grep` 取字段名 → 并进键集合重算（构造 dict 后剔除该字段再比对，即模拟 `hgetall → pop → 打印`）→ 给出"补哪个字段 + 按什么顺序写"（标记行 `DQ`）
- `candidate-rank.mjs` — 多候选择优（1.6.3，纯函数，`CANDIDATES=1` 时 loop 根本不调用它）：`scoreCandidate` 对每份**拼进模板后的提交文本**跑四道闸门打分（`py_syntax`/`empty_block` 各 −100 一票否决、`missing_item` −3、`bad_quote`/`redundant_print` 各 −2），`rankCandidates` 分数降序 + **同分取多数派**（`apiFingerprint` 抽客户端调用序列作比较键）；K 份全都带同一硬缺陷 → `systemic`，作为"多采样救不了、该转实测/人工"的早期信号（比输出指纹早 2~3 轮）。能力边界：`hincrby` 建 hash 这类 Redis 键类型问题本地抓不到，多数派是唯一价值
- `output-diff.mjs` — 输出差异定位器（1.6.4，纯函数）：`splitExpectedActual` 取面板/明细里最后一对「预期输出 / 实际输出」，`diffOutputs` 逐行对齐并**分类**差异（`ORDER_ONLY` 元素同集合仅顺序不同 / `WHITESPACE` 只差空白 / `MISSING_OR_EXTRA` 缺行或多行 / `DICT_ORDER` 键值一一对应只有键序不同 / `VALUE_DIFF` 给第一个不同字符的位置与上下文），并对顺序差异做**机械的排序假设排除**——预期各行是否整体等于字典升序、是否整体等于降序，两者都不成立时直接给出"任何排序策略都无法解释，成因是 Python 2 的 set 迭代顺序被 list() 转出"的结论。两类顺序事故的成因于 2026-09-22 由容器只读实测**定案**（见 `shared/platform-facts.json`）：`DICT_ORDER` 的打印序 = 评测程序 `str(conn.hgetall(k))` 一侧的 py2 哈希表序，**改字典字面量书写序是空操作、改写入序（逐字段 hset / OrderedDict 先后）才换结果**，且是 4 选 1 的落位——要算不要猜；`ORDER_ONLY` 不自洽时正解是真用 `set()` 收集再 `list()`，加 `sort()` 必然错一组。标记写法兼容「预期输出/预期：/【预期】」（含"标记与正文同行"）；`diffSkipReason` 在未启用时把原因打进日志（静默失效必须可见）。`describeOutputDiff` 渲染成注入反思的中文段（无差异/无结构时返回空串）。存在理由：真机见过模型在两坨 2074 字符文本里找了 2.4 万字思考仍没看出"部分行要升序、部分行要降序"这种一眼可判的矛盾。1.6.11 再加 `serverErrorHints`：报错原文命中 `not master and slaveOk=false` / `child process failed` / `Connection refused` 时给出解读——它们是**服务端**在拒绝写或启动失败，指向"被测服务没搭成题面要求的形态"，而不是"你终端连错了对象"（2026-09-23 真机：复制集搭建关 12 轮反思全栽在这个误诊上）。1.6.24 再修一处**误导性分类**：逐行配对在"实际比预期整块少 N 行"时必然错位，而 `show dbs` 这类输出的列宽由**成员数量**决定——真机 13 处差异里有 3 处被标成 `[WHITESPACE] 按预期那一行的空白照抄`（模型对列宽根本无从下手，是不可执行指令），真因是六个库一个都没恢复出来。现在存在 `MISSING_OR_EXTRA` 时 `summarize` 先点名结构性缺行，空白类差异带"很可能是连带结果"的限定
- `loop.mjs` — 编排：生成 → 评测 → 反思循环；`solveOnce` 整体包 `beginRun`/`endRun`（`endRun` 在 `finally`，成功/失败/被停止三态运行态都不悬空）并在各阶段埋 `checkStop` 检查点（意图判定、切工作区、每轮重试开头、**语法守卫修正**、**提交评测前**、反思调用前、重载题目页前）；`watchLoop` 常驻监听；`liteLoop` 刷新触发监听；`courseLoop` 课程自动驾驶；混合题先在命令行做数据准备（输入期报错反思自愈）再落代码分支；代码反思教训链（`lessons` 逐轮注入防横跳）与连续同类报错重载兜底（同一异常 ≥3 轮自动重载题目页重取原始模板，上限 2 次；1.4.3 起重载后先做 `looksLikeOwnDraft` 草稿判据，本平台拿回草稿即不覆盖存档并停用兜底——`slimForReflection` / `looksLikeOwnDraft` 为可单测的纯函数导出）。1.5.0 新增：提交流水线抽出为 `finalizeSubmission`（拼接 → 空区域打点 → shell 护栏 → echo 包裹，反思/守卫修正共用）、写入前过 `py2-guard`、`verdictOf()` 统一处理"本轮结果未确认"的陈旧面板（这类轮次的诊断**不进教训链**）、`lastEvaluatedCode`/`lastEvaluatedCommands` 记账供 `waitEvalResult` 判同错复现。1.5.1 新增：`outputFingerprint` / `pickEvalEvidence` 实际输出指纹——同一输出连续 3 轮即判定"改了等于没改"并终止本题（`output-unchanged`）。1.6.9 起 `finalizeSubmission` 多一道 `stripNonCodeLines` 净化：剔除误入 Begin-End 的「题面对齐表」/ Markdown 表格 / 代码围栏行（表格进代码区会让评测直接 SyntaxError，白烧一轮），**一律保留 `#` 开头的行**（注释在 Python 2 里合法）；配套地，对齐表「被写在代码块内」按**位置错误**处理（`parseAlignmentTable(submitted)` 回退解析）而不再按"漏答"打回重生成。1.6.11 给**命令行分支**补上与代码分支同源的「改了等于没改」闸门（`outputFingerprint` + `STUCK_LIMIT`——此前只接在代码分支上，2026-09-23 真机 12 轮同方向盲试因此无人叫停），并修 `pickEvalEvidence` 只取明细段本身（原先切到文本末尾，把每轮必变的「终端回显」「输入期报错」算进指纹 ⇒ 指纹永不重复、止损形同不存在）；执行前用 `findSquashedHeredocs` 剔除"heredoc 被压成一行"的命令（它们会把后续命令全吞掉）；1.6.12 起命令行/混合两分支的**首次接触**终端先经 `perceiveTerminalEnvReady` 等**内容就绪**再判定环境（`waitForTerminal` 的"可见"不保证"已打印提示符"，切标签后立刻读必得 `unknown`，并连带丢掉客户端实测）。1.6.19 两处：① `finalizeSubmission` 写入前多一道 `stripDestructiveDbStatements`（代码栏是交数据库 eval 的，`remove({})` / `mongoimport` 会真的改掉平台环境且后果延续到之后每一轮）；② **评测失败后复查数据落地**（`recheckDataLanded`，与格式探针同一纪律：切标签 → 等 shell 提示符 → 只读探针 → finally 切回；放在输出指纹之后以免污染比对基准），实测 0 条就走「重置环境 + 重做数据准备」——旧版只在首次提交前核对一次，2026-09-24 真机因此看不见"第 4 轮自己把库删了"，连着 4 轮去改引号形态。1.6.21 两处：① 题面明文提交形态体检（`detectSubmissionFormViolations`）并入 `finalizeSubmission` 流水线**末尾**——守卫/契约打回修复后的文本同样重检，修复不再用 `again.sanitizeNote` 覆盖丢失违约证据（旧版体检只在主路径拼接后跑一次，两条打回路径一覆盖就没）；② `probeMissingPaths` 敲探针前先等 bash 提示符（与格式探针、数据落地复查同一纪律）。1.6.23 三处：① `probeMissingPaths` 改回 `{ note, premiseMissing }`，命令行分支 `premiseStreak` 连续 2 轮命中「前置数据缺失」即止损 `prerequisite-missing`（不等满 `outputFingerprint` 的 3 轮——源数据不在环境里，再烧评测额度全是白试）；② 取证段锚点修复——旧版用首个具体根 `find ${roots[0]}` 做 `lastIndexOf`，而 `/opt/mongodb` 是**后一条**探针命令 `find /opt/mongodb_1` 的前缀子串，会把顶层目录那段（前置缺失判据的唯一证据）整个截掉，现按"第一条探针命令全文 → 顶层根命令片段 → 旧锚点"优先级取锚；③ 课程模式 `solveBoard` 遇 `prerequisite-missing` 即结束本板块（后续关卡依赖同一份前置数据会全部同因失败，不再逐关烧 `MAX_RETRY`）。1.6.24：题面点名的顶层目录**始终补进**取证根清单（回显碎片不再占探针预算），`premiseStreak` 改**锁存**语义（命中累加、只有取证段真看到题面点名的源目录这一正证据才清零——"本轮没判出来"与"本轮证明数据在"是两回事），止损阈值按 `taskCreatesOwnData` 分 1/2 轮（纯恢复题 1 轮即停，旧版把逃生口写成注入文本里的一句话，属最弱的 prompt 层），被跳过的必败命令单独成段进反思材料，`prerequisite-missing` 经 `solveBoard` 的 `stoppedBy` 上抛到课程模式汇总。1.6.25 修**执行层拿过期环境改写命令**：命令行分支改为每轮键入前重新实测 `detectTerminalEnv`（旧版 `termEnv` 是"首轮感知后跨轮复用"，于是判据层已识别 mongosh、模型也按 REPL 写了语句，执行层却按首轮 bash 结论把它们包成 `mongo --eval` 敲进 REPL ⇒ `SyntaxError @(shell):1:8`，三轮同形烧到止损）；另新增 `planReplExit`——实测在 REPL 里而命令是 bash 形态时，只在序列开头插一条 `exit`，不改写任何一条命令
- `cli.mjs` — CLI 入口：`browser | my-edge | probe | dump | caps-check | once | run | watch | lite | course | course-probe | models`；`caps-check` 同时校验能力配置与平台事实档案（`capability-schema.mjs` + `platform-facts-schema.mjs`）
- `inspect-dom.mjs` — 只读 DOM 诊断脚本（关键字命中上下文扫描 + 评测面板结构核对，精调判定规则用）

## 常用命令（均在 `agent/` 目录下执行）

- `npm run probe` — 检查配置、浏览器连接与页面识别（**新平台接入必跑**）
- `npm run dump` — 导出页面结构快照到 `agent/dumps/`（写站点定制规则前先跑）
- `npm run caps-check` — 校验配置类 JSON（编辑 `shared/capabilities/` 或 `shared/platform-facts.json` 后先跑）：能力配置（占位符 ⊆ `paramsSchema.properties`、`required` 一致性）+ 平台事实档案结构（事实段必须带 evidence/date、推断类字样不得混入事实、待验证项只能在 `unknowns`）；`ai.mjs` 加载能力时也会自动预检能力配置
- `npm run once` — 只解当前一题；`npm run run` — 连续解题自动翻页；`npm run watch` — 常驻监听（推荐）；`npm run lite` — 刷新触发：刷新题目页即重做（含反思循环）
- `npm run my-edge` — 以"用户自己的 Edge 配置"重启并带调试端口（junction 绕过 136+ 默认目录限制，保留登录态；会先关闭运行中的 Edge）
- `npm run course` — 课程自动驾驶：遍历「课堂实验→板块→开始学习」逐关完成（前置：浏览器已打开课程列表页）
- `npm run course-probe` — 只读诊断课程列表页识别（不点击），course 卡住时先跑
- `npm run models` — 列出可用文本模型
- `npm run web` — 网页工作台 `http://127.0.0.1:8787`（仅本机可访问：探测 / 解题 / 日志流）
- Windows 用户入口：双击 `agent/start-my-edge.bat`（自己的 Edge）、`agent/start-browser.bat`（独立 profile）、`agent/start-watch.bat`、`agent/start-lite.bat`、`agent/start-course.bat`、`agent/start-web.bat`（网页工作台）

## 约束

- **prompt 单一数据源**：AI 行为的 prompt 只存在于 `shared/capabilities/*.json`，agent 直接读取并渲染 `{{input.xxx}}`。**不得在 agent 内复制 prompt 造成两份漂移**。改 AI 行为优先改 JSON，而非代码。
- **不硬编码站点 selector**：识别全部走通用启发式（编辑器按 Monaco/Ace/CodeMirror/textarea 优先级探测；题干取排除编辑器后最长且偏左的文本块）。针对具体平台调整前先 `npm run dump` 拿真实结构，不要凭假设写规则。
- **数据库/命令行题的环境事实靠实测，不靠模型记忆**：客户端存在性（mongosh/mongo/mysql/redis-cli/psql）与终端形态（bash/REPL）一律实测后进 prompt，缺失客户端做执行层别名替换；**混合题先命令行插入题面文档到指定库（评测环境共享终端数据库，未插入则查询结果为空），代码栏数据库命令题用 `echo "` 双引号包裹裸查询（分号 `;` 分隔、`$`→`\$`）**——平台对代码栏双重执行（bash 环节 + 提取 echo 引号内容做数据库 eval），heredoc/裸语句/`mongo` 前缀实测全失败（1.0.0 生成/反思守则）。
- **写入优先编辑器 API，键盘为回退**：Monaco/CodeMirror5 先 `model.setValue`/`cm.setValue`（触发内容变化事件、平台自动保存不受影响，且字节级精确——实测 Monaco 的 formatOnPaste/autoIndent 会把 insertText 进来的预缩进 Python 逐行重排致评测不匹配）；API 不可用再走 `点击 → Ctrl+A → Delete → insertText`；写入后回读验证；写完点编辑器外部并静置。
- **模板拼接以原始模板为权威 + 兜底直通**：代码题写入 = `spliceIntoTemplate(原始模板, AI 输出)`——仅替换 Begin/End 标记间代码体（按序逐对）、保留行首缩进；**标记不成对或 AI 输出为完整代码（≥2 个模块级语句）时整体直通**（平台只按执行结果评测，宁可整体写入 AI 完整实现，绝不按不可信标记丢函数）；模板存档在函数作用域，连续同类报错 ≥3 轮自动重载题目页重取平台原始模板（1.3.0，购物车/令牌题 IndentationError 死循环根治）——**但该兜底的前提是"重载后编辑器恢复为原始模板"**：EduCoder 系平台持久化草稿、无「恢复初始代码」，重载只能拿回上一轮自提交的草稿，故 1.4.3 加重载后草稿判据（`looksLikeOwnDraft`），命中则保留原存档并停用兜底，绝不拿草稿覆盖干净模板。
- **浏览器连接**：连不上调试端口时自动拉起独立 profile 浏览器（`AUTO_LAUNCH=0` 关闭）；要接管"用户自己的 Edge"须用 `my-edge`（junction 绕过 136+ 默认目录禁令，且必须先关闭运行中的实例——同 profile 旧实例会合并新命令）。沙箱/受限执行环境里脚本拉起的浏览器活不过命令边界，需由资源管理器（双击 bat）启动。
- **手动停止是「协作式中断」，不是 Promise 取消**：中断只能靠 `control.mjs` 的 `checkStop` 在检查点抛 `StopRequested`（playwright 调用无法安全取消，硬断会留下半写入的编辑器/半条命令）。因此**新增长时间等待或新循环时必须补检查点**，否则工作台的「停止做题」会卡到该步骤自然结束；`/api/stop` 不得改走 `withBrowserSession`（队列被在途解题占着，排队等于"点了没反应"）；`beginRun`/`endRun` 必须成对出现（`endRun` 放 `finally`），否则运行态悬空会让按钮永远停在"运行中"。
- **平台事实优先于模型记忆（含 Python 库 API 与运行环境）**：优先级链 = 平台报错回显/实测 > 题面示例写法 > 模板已有代码形态 > 题面描述 > "官方文档/最新版本"的模型记忆；冲突一律以前者为准。教学平台的运行环境常显著落后（**实测 2026-09-20**：Python 2 + 旧版 redis-py——`zadd(key, member, score)` 是「先成员后分值」与 Redis 官方文档相反、`open()` 不接受 `encoding=`、字典式 `zadd(key,{member:score})` 直接报 `ZADD requires an equal number of values and scores`、`int('0\n')` 才能容错换行）。既有原则「数据库/命令行题的环境事实靠实测不靠模型记忆」此前只覆盖 CLI 客户端，**现已扩展到 Python API 形态与运行环境**；prompt 侧对应生成器第 24 条 / 反思器第 15 条。边界：该优先级只用于「接口形态/语法/环境/输出格式/数据格式」；**算法逻辑与题面明文要求仍须正确实现**，不得以"更合理"为名偏离题面。另注意：题面「相关知识」里的 Python 片段常是**数据库命令语义**而非客户端可调用签名（`conn.blpop('a','b',5)` 表达的是 `BLPOP key [key...] timeout`），照抄成 `f(*keys, 5)` 在 Python 2 直接 SyntaxError。
- **评测反馈必须"被观测到"才算数**：`waitEvalResult` 只在**本轮提交与上次送进评测的文本相同**（`lastEvaluatedCode` 记账）时才允许采信"面板与点击前一致"的同错复现；代码变了就等满预算——预算按面板「本关最大执行时间」动态抬高，绝不能用固定 `EVAL_TIMEOUT_MS` 中途放弃。2026-09-22 真机事故链：4 秒采信遗留面板 → 反思拿假证据编出不存在的机制并改出 Python 2 语法都不过的代码 → 第二次点击落在仍在评测的页面上被拦截 → 整题作废（详见 `agent/docs/TROUBLESHOOTING.md` C-12）。推论：`stale` 轮次的诊断不得进 `lessons`，也不得参与"连续同类报错"计数，否则推测会被后续每一轮当成事实吸收。
- **能在本地判定的错误不要花一次评测去换**：平台运行时是 Python 2，一个 SyntaxError 的代价是「写入 + 最长 120 秒评测 + 一轮反思」，而本地检出是毫秒级——写入前一律过 `py2-guard.mjs`，命中即打回修正（最多 2 次后放行提交）。守卫纪律：**宁漏不误报**（误报会让模型横跳）、非 Python 跳过、自身异常 fail-open；语法禁区清单仍留在 `shared/platform-facts.json`，守卫只做"确定性检查"，不替代 prompt。
- **"命令跑完"不等于"命令干活"：静默空操作必须被识别，路径类未知必须去测**：判断一条终端命令成败的常规依据是"有没有报错行"和"退出码"，而 mongorestore 这一类工具会以 `done` 收尾、退出码 0，实际**一条数据都没恢复**（实测回显：`don't know what to do with subdirectory "mongodb_1/test1", skipping...` 后紧跟 `done`）。2026-09-24 真机因此连烧 3 轮——那条最强线索从未进入反思材料，模型转而在没有事实的情况下编出「test1 为空、备份目录是空目录」，而**同一次回显里 `/opt/mongodb/test1/person.bson` 明明恢复了 8 条文档**。两条纪律：① 失败判据认两档，除明确错误语法外还要认"工具自述跳过/不认识/无事可做"（`cmd-evidence.mjs`，只认通用英文语法、不绑定工具名）；② **文件系统与客户端形态一样属"一步就能实测的事实"**，回显里出现"路径不存在/是目录/无法访问"就自动跑只读 `find`/`ls` 取证并把原文塞进反思材料（`probeMissingPaths`），绝不允许"换一个猜测的路径名再花一轮评测"——这条与「数据库/命令行题的环境事实靠实测，不靠模型记忆」同源，只是过去只覆盖了客户端存在性，没覆盖路径与目录布局。推论：**模型写给"环境"的因果结论，若同一批回显里已有反例，判据层必须能看见**（本次的"空目录"说法没有任何机制能否证它，它就被当成了三轮的前提）。③（1.6.23）路径缺失再分一档：**"前置数据缺失"≠"路径名猜错"**——题面引用的源数据目录（如恢复题的 `/opt/mongodb*`）在顶层目录实测清单里**全部缺席**（顶层 `/opt` 为空目录或本身不存在）时，不是"换个写法"能解决的，**任何改写都造不出数据**（`detectMissingPremise`，宁漏不误报、⓪①②③ 四条全中才判；其中 ⓪ 要求参与判定的顶层**必须来自题面**——回显会被 xterm 按列劈行成根本不存在的幽灵顶层根），命中即注入处置段（重做上一关备份 / 重置环境后按课程顺序重做）；命令行分支 `premiseStreak` **命中即累加、只有实测看到题面点名的源目录这一正证据才清零**，命中数达 `premiseLimit`（`taskCreatesOwnData` 判：纯恢复题 1 轮、题面要你自己造数据的题 2 轮）即止损 `prerequisite-missing`（不等满输出指纹 3 轮——源数据不在环境里，再烧评测额度全是白试），课程模式遇该原因直接结束本板块。依据是 2026-09-25 真机：恢复关 3 轮逐字节相同到 `output-unchanged` 止损，但真实卡点（`/opt` 空、备份没落库）从未被点名，止损原因还泛化。
- **"改了等于没改"必须被检测到**：每轮评测失败后对「测试集明细」做归一化指纹（`outputFingerprint` / `pickEvalEvidence` 纯函数），与上一轮相同就往反思材料注入「本地比对提示」并要求换本质不同方向；同一输出出现 3 轮（`STUCK_LIMIT=2` 次重复）直接终止本题（`reason: 'output-unchanged'`）。依据是 2026-09-22 真机：4 轮提交文本各不相同、实际输出逐字节一字未变，AI 在"用列表形式 ↔ 不用列表形式"之间来回摆动，`MAX_RETRY=20 × 128s`（20 是本机 `.env.local` 的配置值，代码默认为 10）的额度会全烧在等价变体上。推论：**环境事实（客户端签名/参数形态）出现争议时用终端实测取证，不要交给模型猜**；且同一条事实**不得既写进事实段又列进 `unknowns`**——"事实 + 待验证"并存等于没有这条事实（1.5.0 的 blpop 条目即因此帮了倒忙，详见 TROUBLESHOOTING C-14）。
- **平台事实单一数据源 = `shared/platform-facts.json`**：所有"平台实测事实"（运行时版本、库 API 形态、评测比对方式、编辑器行为）**只在这里维护**，由 `ai.mjs` 的 `buildPlatformFactsBlock()` 统一注入到每个能力 prompt 末尾（`readCapability` 是唯一注入点，新增能力自动继承）。规则：① 只写实测过的条目，必须带 evidence 与 date；② 推断/待验证写进 `unknowns`，不得混入事实；③ 不缓存、每次读盘（改完即刻生效）；④ 缺文件/坏 JSON 只降级告警，不阻断主流程；⑤ **一次观测只能否证"这次改动有效"，否证不了整个方向**——"改了 X 输出仍逐字节相同"完全可能是两种写法落到同一个哈希落位（字典键序结论 1.6.5 → 1.6.6 → 1.6.7 三连翻转的根因就是这个）。要定"这个东西由什么决定 / 能不能靠代码修"，靠**读真值源**（评测脚本就在容器里：`/data/workspace/myshixun/stepN/read.py`，`from code import *`）或做穷举计算，而不是靠日志归纳出一个猜测再写成事实。**新增事实一律加在这里，不要再散落成 prompt 规则**。
- **prompt 规则默认「通用」，限定作用域必须写明理由**：一条只写给某类任务的好规则，在其他任务里等于不存在。本项目已**两次**栽在同一处——生成器规则 10 与反思器规则 1 都曾把"预期输出不是你要输出的内容"限定为「命令类 / shell 类评测」，于是 Python 代码题（自动补全）里模型合理地判定该规则与本题无关，自己在函数里 print 了评测程序要打印的行，连续多轮反思都在改逻辑、真凶却是多打的 print。**新增规则一律先按通用写；确实只适用某类的，在规则内显式说明适用范围与原因**，并优先考虑把"事实"下沉到 `shared/platform-facts.json`（事实天然通用）。
- **角色设定按"应试者"写，不许给自己发专家奖状**：两个代码 prompt 的开头是"你是应试者，不是软件工程师 / 不是代码评审专家"，唯一评分标准是平台逐行比对的实际输出；随后是三禁（不重构不通用化不顺手优化、不加题面未要求的校验/异常/日志/注解/注释/现代语法糖、取最朴素写法宁可啰嗦）与三条不松的底线（**平台运行时 Python 2 必须编译得过**、Begin/End 标记与行首缩进完整、题面「编程要求」逐条落地）。**新增或改写角色时不得引入"严谨 / 优雅 / 性能 / 健壮 / 最佳实践 / 专家"类措辞**——旧版本里"代码风格严谨，注重性能优化""资深代码评审专家"正是在给专家的冲动发奖状，直接驱动了逐队列 blpop 替代多键 blpop、加防御分支、用 Python 3 现代语法这三类反复出现的失分。要点是"去专家化 ≠ 放松语法"：要朴素贴题面，不是粗糙。
- **能机器判定的，不要交给 prompt**：本项目反复证明"再补一条守则"是约束力最弱的一层（同一类错误被规则写过两次、第三次照样犯）。新增任何"AI 行为规范"前先问一遍：**这条能不能做成确定性检查**——能就写成代码（`py2-guard` 拦语法、`requirement-contract` 拦漏要求与幻觉引用、`wrapDbCommandsInEcho` / `sanitizeShellSubmission` / `wrapBareDbStatementsForShell` 直接改形态、`stripDestructiveDbStatements` 拦删数据的语句、`detectCommandCountViolation` 拦"题面写八条只答七条"、`detectSubmissionFormViolations` 随 `finalizeSubmission` 对每次拼接的最终文本体检形态（1.6.21 起守卫/契约打回修复后的文本也重检，修复不再覆盖丢失违约证据）、`detectShellInvocationViolation` 拦代码栏里的 heredoc / mongo 命令前缀、`outputFingerprint` 拦"改了等于没改"、`format-probe` 用平台的 Python 2 **替模型算出**顺序），把 prompt 留给"确实需要判断力"的部分。**本地算不出的那一小类，去问平台而不是问模型**（1.6.8 的 `format-probe`：容器里 `python -c` 一条纯计算 ≈2 秒，对比一次评测 ≈120 秒）。`requirement-contract.mjs` 里的 `findRedundantPrints` 就是这条的产物：同一坑（把评测程序打印的行复制进被测代码）守则写过两次照样犯，第 1.6.2 版改判据为「编程要求里没有任何输出措辞 → Begin/End 里的 print 一律多余」。配套判据：**规则越少每条越重**，两个代码 prompt 已到 6k 字量级，新增规则要顺手做去重（本项目已做过一次：反思器第 7 条与「解读守则」第 1 条完全重复，压成指针；事实档案里两条讲同一件事的 blpop 条目合并）。**推论（1.6.19，2026-09-24 真机第 5/6/7 轮逐字节相同提交的根因）：同一个形态只能有一层做决定**——执行层兜底（改提交文本）与判据层打回（要求模型改）若各按各的规矩办，模型永远赢不了这场自我辩论，只能产出字节级相同的提交直到止损；这类 bug 的修法是**两处共用同一个判据**（`submissionBody` / `looksLikeDbScript` / `statementPrescribesQuoteForm`），并把它写进 prompt 说明"外层包裹不归你权衡"。**第二个实例（1.6.25，2026-09-25 18:07 真机）**：`[loop] 终端环境识别：mongosh` 紧跟 `[loop] bash 终端：3 条数据库 shell 语句已包成 mongo --eval`——判据层与模型这一轮**都说对了**，执行层拿着首轮过期的 `bash` 结论把正确的 REPL 形态又改回 bash 形态，三轮逐字节同形。推论要补一句：**共用判据不只共用到"同一层规则"，还得共用到"同一时刻的事实"**——环境是每轮会变的（序列里一条 `mongo` 就从 bash 进了 REPL），任何跨轮复用的环境结论都必须就地重测。
- **安全**：端点 / 密钥 / 模型名等个人配置只允许存在于 `agent/.env.local`（已被 .gitignore 排除），源码与 `.env.example` 中不得出现真实值；`agent/.browser-profile*/`（登录态）、`agent/dumps/`（含个人信息的页面快照）、`agent/logs/` 同样严禁提交。
- **如实标注验证边界**：未验证的能力不得在文档中声称可用。
