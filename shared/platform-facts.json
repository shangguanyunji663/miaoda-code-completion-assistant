{
  "_readme": "本平台（教学评测平台，EduCoder 系：www.educoder.net / 校内部署）的**实测事实**单一数据源，由 agent/src/ai.mjs 统一注入到每个能力 prompt 末尾。填写规则：① 只写**实测过**的条目，必须带 evidence（报错/回显原文）与 date；② 推断、待验证的写进 unknowns，**不得混入事实**；③ 冲突裁决顺序：平台报错回显/实测 > 题面示例写法 > 模板已有代码形态 > 题面描述 > 「官方文档/最新版本」的模型记忆；④ 该优先级只覆盖「接口形态 / 语法 / 运行环境 / 输出格式 / 数据格式」——算法逻辑与题面明文要求仍须正确实现，不得以「更合理」为名偏离题面。",
  "platform_id": "educoder-like",
  "runtime": {
    "python": "2.7.6",
    "evidence": "SyntaxError: Non-ASCII character '\\xe5' in file ... but no encoding declared（源文件缺 #-*- coding:utf-8 -*- 时中文注释即炸）；评测输出打印 [u'what', u'when', ...] 为 Python 2 的 unicode repr；open() 不接受 encoding= 参数；容器内实测 python --version → Python 2.7.6（2026-09-22，root@coder 终端）",
    "date": "2026-09-20"
  },
  "redis_py": {
    "zadd_dict_form_supported": false,
    "zadd_positional_order": [
      "member",
      "score"
    ],
    "zadd_evidence": "zadd(key,{member:score}) → redis.exceptions.RedisError: ZADD requires an equal number of values and scores；zadd(key,member,score) 成功写入 80008 条（探针实测 zcard=80008、min=(member, 16777216.0)）；zadd(key,score,member)（Redis 官方顺序）→ ResponseError: value is not a valid float；另经「自动补全」题面示例独立确认：conn.zadd(\"testzset\",\"member2\",3) 即 member2 分值 3（与 Redis 官方 ZADD key score member 相反）",
    "zrevrangebyscore_order": [
      "max",
      "min"
    ],
    "zrevrangebyscore_evidence": "题面示例：四个成员分值 10/70/50/100 调 zrevrangebyscore(key,100,50) 得 member4/member2/member3（按分值降序、含两端）",
    "date": "2026-09-22",
    "zrank_semantics": "zrank(key, member) 返回从 0 开始的排名（升序分值）；题面示例：分值 member3=1/member1=2/member2=3 时 zrank(\"testzset\",\"member2\")=2",
    "zrange_semantics": "zrange(key, start, stop) 按排名区间返回成员，两端**闭区间**；返回 bytes（Python 2 下即 str），例如题面示例 zrange(\"testzset\",0,1) → [\"member3\",\"member1\"]",
    "zrangebylex_on_equal_scores": "分值全相同时，Redis 按**成员字典序**排序——这是前缀补全类题目用「哨兵 + zrank/zrange」或 zrangebylex 的基础",
    "zrange_signature": "只按题面示例的三参数位置形式用：zrange(key, start, stop)（闭区间、按排名）。**不支持 start=/num= 这类关键字参数**——实测（2026-09-20 自动补全题）写 zrange(key, start_rank, end_rank, start=0, num=10) 直接 TypeError: zrange() got multiple values for keyword argument 'start'。要\"取前 N 个\"就在 Python 层切片（items[:10]），不要指望客户端关键字参数",
    "blpop_per_key_loop_is_wrong": "「对每个队列各调一次 blpop」的 for 循环写法在本平台必然超时：每个空队列都会阻塞满 timeout，N 个队列一轮就是 N×timeout，与题面「按列表先后顺序依次检查、弹出第一个非空列表的头元素」（一次调用、按传入顺序检查、全部为空才整体阻塞）不是同一件事。判据：只要代码里出现「循环内对单个键 blpop(…, timeout=10)」就是错的形态，不管外层加不加 sleep/lpop 预检",
    "blpop_per_key_loop_is_wrong_evidence": "2026-09-22 真机（Redis 优先级队列关 tasks/XBLSCWNL/4870）：逐队列 blpop 的四个版本（711/779/932/934 字符，含 lpop 预检版与 time.sleep(1) 版）连续 4 次评测全部耗尽「本关最大执行时间 120 秒」，实际输出停在「测试 pop_task 方法...」之后一字未变（本地比对明细恒为 738 字符）",
    "blpop_signature": "题面「相关知识」里的 Python 片段是 **Redis 命令语义说明，不是 redis-py 可调用签名**：示例 conn.blpop('blist', 'alist', 5) 表达的是「BLPOP key [key...] timeout」（变长键 + 末尾一个数值超时）。而 2.10.6 的签名是 blpop(self, keys, timeout=0)、varargs=None——只接受**一个** keys 实参，所以照抄必然 TypeError，`blpop(*keys, timeout)` 同样不行；多键阻塞弹出的正确写法是把键集合成一个列表传入：conn.blpop(list(keys), timeout=10)（按分值排序后一次性交给 blpop，由 Redis 按传入顺序找第一个非空列表）",
    "blpop_signature_evidence": "容器内实测（2026-09-22，root@coder 终端）：python -c \"import redis,inspect; print redis.__version__; print inspect.getargspec(redis.Redis.blpop)\" → 2.10.6 / ArgSpec(args=['self', 'keys', 'timeout'], varargs=None, keywords=None, defaults=(0,))；题面原文（Redis 优先级队列关「相关知识」）另写明\"给定多个列表时，按列表的先后顺序依次检查\"",
    "version": "2.10.6",
    "version_evidence": "容器内实测 python -c \"import redis; print redis.__version__\" → 2.10.6（2026-09-22）",
    "counters_are_strings": "题面说「对计数器 X 递增 1，并取递增后的值」时，X 是 **string 键**：只能用 conn.incr(\"X\")（incr 的返回值就是递增后的整数，不需要再读一次）。不要用 hincrby —— 它会把 X 建成 hash，而评测程序按 string 读它（conn.get），当场 WRONGTYPE 中断整轮评测",
    "counters_are_strings_evidence": "2026-09-22 真机（反向索引关）：index_document 里写 conn.hincrby(\"content:id\", \"num\", 1)，评测回显 Traceback (most recent call last): File \"step1/read.py\", line 40, in <module> print \"当前全文编号: %s\"%(conn.get('content:id')) … redis.exceptions.ResponseError: WRONGTYPE Operation against a key holding the wrong kind of value；题面「相关知识」另给出 conn.incr(\"testcount\") → 执行后 testcount '1'",
    "harness_prints_are_not_ours": "同关实测：评测程序 step1/read.py 自己打印「当前全文编号 / 当前全文详情 / 当前索引词 / 索引词: keyword:…」全部四组行（第 40 行即其中一条 print）。被测函数里再打一遍 → 实际输出里这些行按调用次数成倍出现，逐行比对必然不匹配。「编程要求」里没有打印要求时，一个字都不要输出",
    "harness_prints_are_not_ours_evidence": "2026-09-22 真机（反向索引关）：index_document 内自带 4 行 print，实际输出里「索引词: keyword:…」整段重复出现 3 次（每题面输入行调用一次），而 step1/read.py 自己那份因 WRONGTYPE 未打出",
    "hash_field_print_order_is_py2_dict_table": "面板里 `str(conn.hgetall(key))` 的键顺序 = **Python 2 dict 的哈希表序**，链路：被测代码写入字段的先后 → 小哈希按写入序回包 → redis-py 按回包序构造**普通 dict** → 按表序打印，即打印序 = f(键集合, 写入序)。三条推论：① **改字典字面量的书写顺序基本是空操作**（送进 hmset 前已被 py2 打乱一次）；② **OrderedDict / 逐字段 hset 能改键序**，但只是在有限的几种落位之间换一格，不是「按示意写就按示意打印」；③ 键集合本身也参与决定结果（多写/少写一个字段会整体改变其余字段落位）。目标顺序要**算**不要猜：容器里跑 `python -c \"print dict([(k,i) for k,i in zip(顺序,range(n))]).keys()\"`，取打印出来等于预期键序的那个写入序（无需 Redis、无需评测）",
    "hash_field_print_order_is_py2_dict_table_evidence": "2026-09-22 容器内只读实测（微博用户/动态关 tasks/XBLSCWNL/4868，命令行标签执行 python -c）：6 键 login_name/posts/real_name/followers/following/id 的 720 种插入顺序只产生 4 种 `.keys()` 结果，其中 120 种等于面板预期序 `login_name, posts, real_name, followers, following, id`；按题面示意顺序插入得到 `posts, login_name, followers, following, real_name, id`，与该关真机实际输出逐字吻合（存档 code.py 第 18 行确为按示意顺序的 OrderedDict）；另测「7 键（含 read.py 会 pop 掉的 last_signup）按示意插入后 pop」得到第三种结果 `id, posts, real_name, followers, following, login_name`"
  },
  "python2_syntax": {
    "note": "以下形态在平台运行时（Python 2，见 runtime 段）会**直接 SyntaxError 使整轮评测失败**——不是风格问题，是能否编译的问题。由 runtime 段的实测结论（平台为 Python 2）加语言规范推导，agent 侧另有提交前的本地静态守卫（agent/src/py2-guard.mjs）在写入前拦这些形态",
    "star_then_positional": "`f(*a, x)`（*解包 之后再接位置实参）属 PEP 448「Additional Unpacking Generalizations」，Python 3.5+ 才支持；Python 2 只允许 `f(*a)`、`f(**kw)`、`f(*a, k=1)`、`f(*a + [x])`。多键命令的等价写法：把参数拼进一个列表再解包，或直接传列表给客户端方法",
    "star_then_positional_evidence": "PEP 448 归属 Python 3.5；平台运行时 Python 2 的实测证据见 runtime 段（SyntaxError: Non-ASCII character… / open() 不接受 encoding= / 打印 [u'…'] unicode repr）",
    "other_forbidden": [
      "f-string：`f\"{x}\"`（3.6+）→ 用 % 或 .format()",
      "海象运算符 `:=`（3.8+）",
      "类型注解：`def f(x: int) -> str`、变量注解 `x: int = 1`（3.0+/3.6+）",
      "仅关键字参数分隔符 `def f(a, *, b)`（3.0+）",
      "`nonlocal`（3.0+）、`yield from`（3.3+）、`async def`/`await`（3.5+）、`raise X from Y`（3.0+）、`except*`（3.11+）",
      "无参 `super()`（3.0+）：Python 2 必须写 `super(类名, self)`，否则运行时 TypeError",
      "列表/字典字面量内的解包 `[*a, b]`、`{**d, \"k\": v}`（3.5+）"
    ],
    "comments_must_be_ascii_safe": "源文件若无 `-*- coding:utf-8 -*-` 声明，中文注释即报 SyntaxError: Non-ASCII character——模板首两行的编码声明必须原样保留（见 runtime 段证据）",
    "date": "2026-09-22"
  },
  "python_stdlib": {
    "open_encoding_param": false,
    "evidence": "TypeError: 'encoding' is an invalid keyword argument for this function",
    "date": "2026-09-20",
    "set_iteration_order": "Python 2 的 `set` 迭代顺序由元素哈希决定——**既不是插入序也不是字典序**，但对同一元素集合稳定可复现。题面用「集合」二字描述收集方式的地方，参考实现就是真用 set 再 `list()` 转换，所以预期输出会出现「这组要升序、那组要降序」这种任何排序策略都自相矛盾的形态。正解：照题面用 set 收集后 list() 转换；**不要给 list 加 sort()/sorted()**（两组顺序不可能同时由排序得到，加了必然错一组）。要哪一组的确切顺序不必猜，容器里一条命令算得出：`python -c \"print list(set(['refactor','refactoring']))\"`",
    "set_iteration_order_evidence": "2026-09-22 容器内只读实测（root@coder 的 bash 提示符）：`python -c \"print list(set(['code','coding'])); print list(set(['refactor','refactoring'])); print list(set(['test','testing']))\"` → ['code', 'coding'] / ['refactoring', 'refactor'] / ['test', 'testing']，与检索式解析关（tasks/XBLSCWNL/4869）面板预期 `([['software'], ['code', 'coding'], ['refactoring', 'refactor']], ['test', 'testing'])` 三处顺序逐字吻合。该关此前 5 轮反思依次改过「剥 + 前缀」「分组触发条件」「加 sort() 按字典序」，第 4、5 轮实际输出逐字节相同（指纹 8327b08638）",
    "set_iteration_order_date": "2026-09-22"
  },
  "evaluation": {
    "stdout_comparison": "line-by-line",
    "multi_test_sets": true,
    "prints_sorted_set_cardinality": true,
    "cleanup_note": "面板末尾评测程序会执行 conn.delete(*to_del)；当有序集合为空时该调用报 wrong number of arguments for 'del' command",
    "evidence": "面板「—— 预期输出 —— / —— 实际输出 ——」逐行比对；出现「测试集1 / 测试集2」多组；打印 Redis ip2city sorted set ranges: 80008",
    "date": "2026-09-20",
    "repr_form_matters": "Python 2 下 print 的列表/元组 repr 会因元素类型而不同：bytes(str) 打印 [\"what\"]，unicode 打印 [u\"what\"]；同理元组 (u\"wg{\",...) 会带 u 前缀。**因此返回值是 bytes 还是 unicode 会直接改变 stdout 文本，进而影响逐行比对**——必须按题面「预期输出」的形态决定是否 decode",
    "return_shape_matters": "题面预期输出形如 (\"wg{\", \"wh{\")，说明该函数须返回 **tuple**（若返回 list 会打印成 [...]，与预期不符）",
    "harness_owns_all_prints": "评测程序的 stdout 全部由评测程序（read.py 之类）自己打印——包括 \"Test autocomplete, prefix: 'wh'\"、\"The start/end range of 'wh' is:  ('wg{', 'wh{')\"（注意 is: 后有**两个空格**，是 print(a, b) 逗号形式）、\"Add keyword: who\" 等。**被测代码绝对不能自己 print 这些行**，否则输出重复 → 逐行比对必然不匹配。实测（2026-09-20 自动补全题）：函数里自加这两行 print 的版本反复失败；去掉全部 print 后**一次提交即通过**",
    "return_bytes_not_unicode": "Python 2：zrange 等命令返回 bytes（即 str），直接返回/打印得到 ['what', ...]；若 decode 成 unicode 再打印会变成 [u'what', ...] —— **文本不同即判不匹配**。实测：\"不 decode、原样返回\" 通过；此前带 u 前缀的版本失败",
    "no_selftest_block_needed": "无需在提交文件里写 if __name__ == \"__main__\" 自测块：评测程序会 import code.py 并直接调用函数（该块在 import 时不会执行），写了属无害噪声，但常见的两类副作用是\"自加 print\"与\"自加 conn.delete\"",
    "harness_script_path_and_reset": "每关的评测程序是容器内的 `/data/workspace/myshixun/stepN/read.py`：`from code import *` 引入被测代码，自己 print 全部结果行，评测前后各做一次重置（只删它列出的那些键）。对被测代码三条后果：① 脚本里 `pop(\"字段\", \"404\")` 掉、预期输出里却看不到的字段，是参考实现**写了**的字段——**键集合要与参考一致**，漏写会连带改变其余字段的打印落位；② 被测函数只需 return，打印归脚本（见 harness_owns_all_prints）；③ 容器内 redis **不常驻**（命令行里 `redis-cli ping` = Connection refused），需要在终端连 Redis 时自己 `redis-server > /dev/null 2>&1 &` 起一个，评测脚本本身也是这么起的",
    "harness_script_path_and_reset_evidence": "2026-09-22 容器内只读实测（微博用户/动态关 tasks/XBLSCWNL/4868 的「命令行」标签，bash 提示符 root@coder）：`find /data -maxdepth 4 -name read.py` → step1/2/3；`sed -n` 通读 step1/read.py 全文 67 行；`grep -c create_user step*/read.py` 定位本关为 step1；`redis-cli ping` → Connection refused；`tail -12 /data/workspace/eva.log` → 空",
    "harness_script_path_and_reset_date": "2026-09-22",
    "detail_panel_two_column_layout": "评测明细里「—— 预期输出 ——」与「—— 实际输出 ——」是**并排渲染在同一行的两个栏目标记**，两份正文并不跟在各自标记之后，而是**整块挂在标题行的下一个兄弟元素**里、且是**两个并列子元素**（顺序：第 1 个=预期、第 2 个=实际）。按 innerText 位置切分会把预期段切成两个标记之间的「——」（被清空），两份正文被「实际输出」一并吞掉 → `expected` 为空 → 差异定位与容器格式反解**同时失效**。取法：文本锚定同时含两个栏目名的**最小元素** → 取 `nextElementSibling` 的子元素（≥2 个且量级相当才认）；**不要写死带 hash 的 CSS module 类名**（实测形如 `output-title-container___P2NjC`，随构建变）",
    "detail_panel_two_column_layout_evidence": "2026-09-23 只读 DOM 取证（微博用户/动态关 tasks/XBLSCWNL/4868）：`LI.test-case-item` > `DIV.item-desc` 的两个子元素为 `P.output-title-container`（innerHTML = `<span>—— 预期输出 ——</span><span class=\"output-title___ZQJkv\"><span>—— 实际输出 ——</span><span class=\"c-blue current\">展示原始输出</span></span>`）与 `DIV.diff-panel-container___IpXsK`；后者含 2 个子 div、各 552 字符：第 1 个 dict 序为预期序 `login_name,posts,real_name,...`、第 2 个为实际序 `posts,login_name,...`。旧切分产出的明细恒为 1117 字符且 `expected` 为空，与 09-22 18:44–18:46 三次逐字相同",
    "detail_panel_two_column_layout_date": "2026-09-23",
    "popped_fields_are_auto_probeable": "「评测脚本会 pop 掉参考实现写了而预期输出看不到的字段」这一条**可由 agent 只读自动取证**：`grep -ho 'pop(\"[^\"]*\"' /data/workspace/myshixun/step*/read.py` 取字段名，再把候选字段并进键集合枚举、构造 dict 后**剔除该字段**比对预期序（即模拟 `hgetall → pop → 打印`）。**0 解正是「字段集合与参考不同」的判据**——只按可见键集合怎么排都到不了预期序时，补上被 pop 的字段立刻有解",
    "popped_fields_are_auto_probeable_evidence": "2026-09-23 容器内 python -c 纯计算 + 真机一次通过：`DP 1 120 184`（6 键 720 种中 120 解）/ `DP 2 0 -1`（4 键 24 种**全不可行**）；`grep` read.py → `pop(\"last_signup\"` 与 `pop(\"posted\"`（step1 第 36/52 行）；补字段重算 → `RES6 1200 873`（7 键 5040 种中 1200 解）/ `RES4 24 9`（5 键 120 种中 24 解）；agent 自动执行该链路后模型补 `posted` 并按 `content → user_name → id → posted → uid` 写入，第 4 次尝试命中「恭喜您通过本关」",
    "popped_fields_are_auto_probeable_date": "2026-09-23"
  },
  "editor": {
    "draft_persisted_by_platform": true,
    "reset_button_available": false,
    "evidence": "写入平台原始模板后重新 goto 同一题目页，编辑器仍是上一次的草稿（891 字符）；页面可点击元素只有 上一关/自测运行/评测 + 章节标签，无「恢复初始代码」",
    "date": "2026-09-20"
  },
  "unknowns": [
    "zrangebylex 是否可用（本题面未使用它；用的是 zrank + zrange 哨兵法）",
    "zadd 的 score 是否接受 int（题面示例用 int 3/0，实测待单独确认；当前证据表明接受）",
    "题面「预期输出」是否为字节级精确（本次已用实测判定 u 前缀问题，但其他空白差异未逐一验证）",
    "python2_syntax 段各形态的 SyntaxError 原文（依据是 runtime 段实测的 Python 2 + 语言规范，尚未拿到本平台的报错回显逐条留证）",
    "/opt/collection_1 与 /opt/collection_2 的**真实目录布局**：2026-09-24 那一关三轮分别猜过 /opt/collection_1/person/person.bson、/opt/collection_1/person.bson、以及「目录 + -c」，全部落空，而**全程没有 ls 过一次**。一条 `find /opt -maxdepth 4` 即可定案（已接线为失败轮的自动只读取证，但该命令本身还没在真机上跑过）",
    "「整库备份目录恢复成另一个名字的库」的正确写法到底是哪一种：题面示例形态是把目标指到内层库目录（`-d test /home/mongod/test`），还是 `--nsInclude/--nsFrom/--nsTo` 命名空间重映射，或 `--gzip`/`--dir` 组合——只观测到「外层目录 + `-d`」会被静默跳过，**成功形态一次都没有实测到**",
    "检索式解析关改成「用 set 收集同义词后 list() 转换」之后整题能否通过：三组顺序已实测定案为 py2 set 迭代序，但从未按该写法真机提交过一次（该关 5 轮额度当时已烧完）",
    "大哈希（字段数或值长度超过 Redis ziplist 阈值）时 hgetall 回包顺序是否仍等于写入顺序——本档案的链路结论来自「纯 Python 2 模拟预测的键序与真机实际输出逐字吻合」这一间接印证，未在 Redis 侧直接观测（容器内 redis-server 评测时才起，起服务属改动环境，未做）",
    "微博关参考实现究竟按哪种写入顺序（**已不构成卡点**：该关 2026-09-23 真机通过——`user:{id}` 按可见 6 键的某个解、`post:{id}` 补 posted 字段后即匹配；参考实现的具体写法仍未直接取证，也不需要了）",
    "【代码栏形态，2026-09-24 起待定案】mongo/Redis 查询类关卡的正解到底是哪一种：(A) `echo \"` 双引号包裹整段 + 语句内单引号 + `\\$`；(B) 题面明文那种不带 echo 的裸语句 + `\\$` + 单引号。目前只观测到 **(A) 能把命令送进数据库 eval**（两个 count 标签各打印 0），(B) **从未真正提交过一次**——每一轮都被 `wrapDbCommandsInEcho` 改写成 (A)，因此\"(B) 才是该关正解\"从来没有证据。旧版把这条当作事实写（\"该形态与 echo 包裹互斥、题面明文优先\"），依据只有题面措辞本身，2026-09-24 已降级到这里；定案需要一次真机各提交一版并留下面板原文",
    "person.json 到底是不是 JSON 数组：代码栏里跑 `mongoimport` 回显 `bad JSON array format - found no opening bracket '['`（暗示是逐行 JSON、需 `--jsonArray`），而命令行里不带 `--jsonArray` 却 `imported 8 documents`（暗示是数组）。两条观测指向不同，未单独取证；注意代码栏里那句本来就不该存在（shell 命令不进代码栏）"
  ],
  "code_block_db_task": {
    "note": "代码栏（Begin-End）写数据库操作命令的题：代码栏内容**先过 bash、再被平台提取引号内文本交数据库 shell eval**（错误回显分别形如 step3/query.sh: line N: syntax error 与 @(shell eval):1:N）。两种执行环节都在，所以『echo 双引号包裹』与『题面明文的裸语句』是否等价**尚未定案**（见 unknowns），当前判据：外层包裹由执行层兜底决定，题面的引号要求只管语句内部的字符串字面量。",
    "exec_via_db_eval": true,
    "exec_via_db_eval_evidence": "2026-09-23 15:20 真机 /tasks/XBLSCWNL/4883：实际输出回显 `E QUERY [js] SyntaxError: missing ; before statement @(shell eval):1:14`",
    "echo_wrapped_body_reaches_db_eval": true,
    "echo_wrapped_body_reaches_db_eval_evidence": "2026-09-24 12:59 真机 /tasks/XBLSCWNL/4883 step3：代码栏写 `echo \"` + 分号分隔的 7 条 db.getSiblingDB 语句（含 `\\$`），实际输出里第 7、8 个标签后各打印 `0`——count() 被执行了（0 是因为集合当时被我方 remove 删空），说明引号内内容确实被提取并逐条 eval；bash 环节没有把整段原样吐进实际输出",
    "code_block_statements_mutate_real_environment": true,
    "code_block_statements_mutate_real_environment_evidence": "2026-09-24 12:58 同关：代码栏里那句 `db.test.remove({})` 在实际输出回显 `WriteResult({ \"nRemoved\" : 8 })`——**平台提供、且我方已在命令行导入并实测过 8 条的文档被真的删掉了**，此后每轮 count 恒为 0。代码栏不是沙箱，写进去的删除语句后果延续到之后所有轮次",
    "shell_cmds_must_not_be_in_code_block": true,
    "shell_cmds_must_not_be_in_code_block_evidence": "同上：代码栏混入 `mongoimport --db mydb2 --collection test --file …`（shell 命令）后整份 eval 解析失败，面板 7 个查询标签后全部为空",
    "test_panel_has_two_bodies": true,
    "test_panel_has_two_bodies_evidence": "同上：面板标题行 `p.output-title-container` 的下一个兄弟 `div.diff-panel-container` 里有**两个并列 body**（预期 2492 字符 / 实际 267 字符）；实际输出因报错比预期短一个数量级，抓取侧曾按「两列量级比 > 0.3」拒绝对而误判成\"预期段为空\"",
    "date": "2026-09-24"
  },
  "mongo_dump_restore_task": {
    "note": "mongodump/mongorestore 关卡（容器内 MongoDB 4.0.0 + mongo-tools，终端为 bash）。核心风险是 mongorestore 的**静默空操作**：它以 `done` 收尾、退出码 0，实际一条数据都没恢复，回显里连 error 字样都没有——只按「有没有报错」判断命令成败会完全失灵。",
    "dir_target_with_dash_db_is_silently_skipped": true,
    "dir_target_with_dash_db_is_silently_skipped_evidence": "2026-09-24 14:18 真机 /tasks/XBLSCWNL/4882（mongorestore 关）终端回显原文：`mongorestore -h 127.0.0.1:27017 -d mytest1 --drop /opt/mongodb_1` → `the --db and --collection args should only be used when restoring from a BSON file...` → `building a list of collections to restore from /opt/mongodb_1 dir` → `don't know what to do with subdirectory \"mongodb_1/test1\", skipping...` → `done`。整库备份目录里还有一层库目录（test1/），mongorestore 直接跳过整个子目录、零恢复量，但退出码 0；mytest1/mytest4 两条同形同结果",
    "collection_flag_with_directory_target_is_rejected": true,
    "collection_flag_with_directory_target_is_rejected_evidence": "同关卡：`mongorestore -d mytest2 -c person --drop /opt/collection_1` → `checking for collection data in /opt/collection_1` → `Failed: error scanning filesystem: file /opt/collection_1 is a directory, not a bson file`（这一条**有** Failed 字样，与上一条的静默形成对照）",
    "whole_dump_dir_restore_succeeds_without_dash_db": true,
    "whole_dump_dir_restore_succeeds_without_dash_db_evidence": "同关卡：`mongorestore -h 127.0.0.1:27017 --drop /opt/mongodb` → `reading metadata for test1.person from /opt/mongodb/test1/person.metadata.json` → `finished restoring test1.person (8 documents)`、`finished restoring test2.student (8 documents)` → `done`。即 ① 备份数据本身是完好的（8 条 ×2），② 不带 `-d` 的整目录恢复是走得通的。这直接否证了模型当轮写下的「test1 为空、mongodb_1 与 collection_1 是空目录」——它连 ls 都没跑过",
    "rerun_without_drop_yields_duplicate_key": true,
    "rerun_without_drop_yields_duplicate_key_evidence": "同关卡 14:22：把 `--drop` 摘掉重跑 → `error: multiple errors in bulk operation: - E11000 duplicate key error collection: test1.person index: _id_ dup key: { : 1 }`（1..8 全列），末尾仍打 `finished restoring test1.person (8 documents)` + `done`。含义是**数据已经在库里**，不是连接或客户端问题；为消掉这行报错而去掉 --drop 反而会让面板状态与预期无关",
    "date": "2026-09-24"
  },
  "task_env": {
    "reset_entry": "命令行标签栏右侧的工具栏按钮（DOM: a[title=\"工具栏\"]，图标 icon-gongjuxiang）→ 弹出「功能」菜单 → 「重置环境」",
    "reset_entry_evidence": "2026-09-23 用户截图指路 + 只读 DOM 探针确认（tabbar 行 action-bar 内 action-bar___bbigU 下有 a[title=\"工具栏\"] 与 a[title=\"全屏\"]；菜单项文本为「重置环境」，同级还有 上传文件/下载文件/服务预览/SSH直连）",
    "reset_restores_environment": true,
    "reset_restores_environment_evidence": "同上菜单标题为「功能」；「关卡配置信息」区显示 跳关=允许、测试集解锁=不允许，说明该入口属平台关卡级功能。用户原话：应当用它重置，\"而不是刷新页面\"",
    "page_reload_does_not_restore_env": true,
    "page_reload_does_not_restore_env_evidence": "2026-09-23 真机：重载题目页后编辑器仍是上一轮提交的草稿（平台持久化草稿、无「恢复初始代码」），且被覆盖的输入文件 /home/example/person.json 与数据库内容都不恢复",
    "date": "2026-09-23"
  }
}
